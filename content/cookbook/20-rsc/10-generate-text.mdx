---
title: 生成文本
description: 学习如何使用 AI SDK 和 React Server Components 生成文本
tags: ['rsc']
---

# 生成文本

<Note>
  本示例使用 React Server Components (RSC)。若需要客户端渲染与 hooks，请参阅[使用 useState 的「生成文本」示例](/examples/next-pages/basics/generating-text)。
</Note>

有时你需要根据提示生成文本，例如回答问题或总结一段文字。可使用 `generateText` 根据输入提示生成文本。

<Browser>
  <TextGeneration />
</Browser>

## 客户端

创建一个简单的 React 组件：点击按钮时调用 `getAnswer`。`getAnswer` 会调用 `ai` 模块的 `generateText`，根据输入提示生成文本。

```tsx filename="app/page.tsx"
'use client';

import { useState } from 'react';
import { getAnswer } from './actions';

// Allow streaming responses up to 30 seconds
export const maxDuration = 30;

export default function Home() {
  const [generation, setGeneration] = useState<string>('');

  return (
    <div>
      <button
        onClick={async () => {
          const { text } = await getAnswer('Why is the sky blue?');
          setGeneration(text);
        }}
      >
        Answer
      </button>
      <div>{generation}</div>
    </div>
  );
}
```

## 服务端

在服务端实现 `getAnswer`，调用 `ai` 模块的 `generateText`，根据输入提示生成文本。

```typescript filename='app/actions.ts'
'use server';

import { generateText } from 'ai';

export async function getAnswer(question: string) {
  const { text, finishReason, usage } = await generateText({
    model: 'openai/gpt-5.4',
    prompt: question,
  });

  return { text, finishReason, usage };
}
```
