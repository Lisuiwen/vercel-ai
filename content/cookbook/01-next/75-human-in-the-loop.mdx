---
title: Next.js 人机协同（HITL）
description: 在 Next.js 与 AI SDK 中为 agent 系统添加人工审批步骤
tags: ['next', 'agents', 'tool use']
---

# Next.js 人机协同（HITL）

构建 agent 系统时，宜加入人机协同（HITL），让用户在系统执行操作前审批。AI SDK 通过工具上的 `needsApproval` 属性内置支持工具执行审批。

本示例展示如何在 Next.js 聊天机器人中使用 AI SDK 原生工具执行审批。

## 背景

理解实现方式前，先看 AI SDK 在 Next.js 聊天应用中的工具调用流程。

前端使用 `useChat` hook 管理消息状态与用户交互。

```tsx filename="app/page.tsx"
'use client';

import { useChat } from '@ai-sdk/react';
import { DefaultChatTransport } from 'ai';
import { useState } from 'react';

export default function Chat() {
  const { messages, sendMessage } = useChat({
    transport: new DefaultChatTransport({
      api: '/api/chat',
    }),
  });
  const [input, setInput] = useState('');

  return (
    <div>
      <div>
        {messages?.map(m => (
          <div key={m.id}>
            <strong>{`${m.role}: `}</strong>
            {m.parts?.map((part, i) => {
              switch (part.type) {
                case 'text':
                  return <div key={i}>{part.text}</div>;
              }
            })}
            <br />
          </div>
        ))}
      </div>

      <form
        onSubmit={e => {
          e.preventDefault();
          if (input.trim()) {
            sendMessage({ text: input });
            setInput('');
          }
        }}
      >
        <input
          value={input}
          placeholder="Say something..."
          onChange={e => setInput(e.target.value)}
        />
      </form>
    </div>
  );
}
```

后端创建使用 `streamText` 并返回 `UIMessageStreamResponse` 的路由处理器。工具含 `execute` 函数，模型调用时自动执行。

```ts filename="app/api/chat/route.ts"
import { streamText, tool } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

export async function POST(req: Request) {
  const { messages } = await req.json();

  const result = streamText({
    model: openai('gpt-4o'),
    messages,
    tools: {
      getWeatherInformation: tool({
        description: 'show the weather in a given city to the user',
        inputSchema: z.object({ city: z.string() }),
        execute: async ({ city }) => {
          const weatherOptions = ['sunny', 'cloudy', 'rainy', 'snowy'];
          return weatherOptions[
            Math.floor(Math.random() * weatherOptions.length)
          ];
        },
      }),
    },
  });

  return result.toUIMessageStreamResponse();
}
```

用户向 LLM 询问纽约天气时，模型生成带城市参数的工具调用，AI SDK 自动运行 `execute` 并返回结果。

要加入 HITL 步骤，在工具调用与执行之间用 `needsApproval` 设置审批门控。

## 添加工具执行审批

### 服务端配置

在工具定义中添加 `needsApproval: true`。工具仍保留 `execute`，但 SDK 会暂停执行直至用户批准。

```ts filename="app/api/chat/route.ts" highlight="14"
import { streamText, tool } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

export async function POST(req: Request) {
  const { messages } = await req.json();

  const result = streamText({
    model: openai('gpt-4o'),
    messages,
    tools: {
      getWeatherInformation: tool({
        description: 'show the weather in a given city to the user',
        inputSchema: z.object({ city: z.string() }),
        needsApproval: true,
        execute: async ({ city }) => {
          const weatherOptions = ['sunny', 'cloudy', 'rainy', 'snowy'];
          return weatherOptions[
            Math.floor(Math.random() * weatherOptions.length)
          ];
        },
      }),
    },
  });

  return result.toUIMessageStreamResponse();
}
```

模型调用该工具时，SDK 不向客户端执行 `execute`，而是发送状态为 `approval-requested` 的工具部分。用户响应后才执行工具。

### 客户端审批 UI

前端检测 `approval-requested` 状态并渲染批准/拒绝按钮。使用 `useChat` 的 `addToolApprovalResponse` 发送用户决定。

```tsx filename="app/page.tsx"
'use client';

import { useChat } from '@ai-sdk/react';
import {
  DefaultChatTransport,
  lastAssistantMessageIsCompleteWithApprovalResponses,
} from 'ai';
import { useState } from 'react';

export default function Chat() {
  const { messages, sendMessage, addToolApprovalResponse } = useChat({
    transport: new DefaultChatTransport({
      api: '/api/chat',
    }),
    sendAutomaticallyWhen: lastAssistantMessageIsCompleteWithApprovalResponses,
  });
  const [input, setInput] = useState('');

  return (
    <div>
      <div>
        {messages?.map(m => (
          <div key={m.id}>
            <strong>{`${m.role}: `}</strong>
            {m.parts?.map((part, i) => {
              if (part.type === 'text') {
                return <div key={i}>{part.text}</div>;
              }
              if (part.type === 'tool-getWeatherInformation') {
                switch (part.state) {
                  case 'approval-requested':
                    return (
                      <div key={part.toolCallId}>
                        Get weather information for {part.input.city}?
                        <div>
                          <button
                            onClick={() =>
                              addToolApprovalResponse({
                                id: part.approval.id,
                                approved: true,
                              })
                            }
                          >
                            Approve
                          </button>
                          <button
                            onClick={() =>
                              addToolApprovalResponse({
                                id: part.approval.id,
                                approved: false,
                              })
                            }
                          >
                            Deny
                          </button>
                        </div>
                      </div>
                    );
                  case 'output-available':
                    return (
                      <div key={part.toolCallId}>
                        Weather in {part.input.city}: {part.output}
                      </div>
                    );
                  case 'output-denied':
                    return (
                      <div key={part.toolCallId}>
                        Weather request for {part.input.city} was denied.
                      </div>
                    );
                }
              }
            })}
            <br />
          </div>
        ))}
      </div>

      <form
        onSubmit={e => {
          e.preventDefault();
          if (input.trim()) {
            sendMessage({ text: input });
            setInput('');
          }
        }}
      >
        <input
          value={input}
          placeholder="Say something..."
          onChange={e => setInput(e.target.value)}
        />
      </form>
    </div>
  );
}
```

Here's how the approval flow works:

1. The model calls `getWeatherInformation` with a city
2. The tool part enters the `approval-requested` state with an `approval.id`
3. The UI renders approve/deny buttons
4. When the user clicks a button, `addToolApprovalResponse` records the decision
5. `sendAutomaticallyWhen` detects all approvals are responded to and sends the message
6. On the server, if approved, the `execute` function runs and returns the result. If denied, the model receives the denial and responds accordingly.

### 审批后自动提交

`sendAutomaticallyWhen` 配合 `lastAssistantMessageIsCompleteWithApprovalResponses` 可在上一步所有工具审批均已响应后自动发送消息；否则每次审批后需手动调用 `sendMessage()`。

```tsx
import { useChat } from '@ai-sdk/react';
import { lastAssistantMessageIsCompleteWithApprovalResponses } from 'ai';

const { messages, addToolApprovalResponse } = useChat({
  sendAutomaticallyWhen: lastAssistantMessageIsCompleteWithApprovalResponses,
});
```

<Note>
  批准工具执行后若无反应，请手动调用 `sendMessage` 或在 `useChat` 上配置 `sendAutomaticallyWhen`。
</Note>

### 动态审批

可向 `needsApproval` 传入异步函数，根据工具输入决定是否需审批：

```ts filename="app/api/chat/route.ts" highlight="15"
import { streamText, tool } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

export async function POST(req: Request) {
  const { messages } = await req.json();

  const result = streamText({
    model: openai('gpt-4o'),
    messages,
    tools: {
      processPayment: tool({
        description: 'Process a payment',
        inputSchema: z.object({
          amount: z.number(),
          recipient: z.string(),
        }),
        needsApproval: async ({ amount }) => amount > 1000,
        execute: async ({ amount, recipient }) => {
          return `Payment of $${amount} to ${recipient} processed.`;
        },
      }),
    },
  });

  return result.toUIMessageStreamResponse();
}
```

本示例中仅超过 $1000 的支付需审批，较小金额自动执行。

### 处理拒绝

用户拒绝工具执行时，模型会收到拒绝信息并相应回复。为防止模型重试同一工具调用，可添加指令：

```ts highlight="5-6"
const result = streamText({
  model: openai('gpt-4o'),
  messages,
  system:
    'When a tool execution is not approved by the user, do not retry it. ' +
    'Inform the user that the action was not performed.',
  tools: {
    // ...
  },
});
```

## 完整示例

可在 AI SDK 仓库的 [`next-openai` 示例](https://github.com/vercel/ai/tree/main/examples/next-openai) 中查看运行效果，访问 `/test-tool-approval` 页面及对应路由处理器。

工具执行审批详见[工具执行审批](/docs/ai-sdk-core/tools-and-tool-calling#tool-execution-approval)与[聊天机器人工具用法](/docs/ai-sdk-ui/chatbot-tool-usage#tool-execution-approval)文档。
