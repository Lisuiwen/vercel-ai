---
title: Express
description: 学习如何在 Express 服务器中使用 AI SDK
tags: ['api servers', 'streaming']
---

# Express

你可以在 [Express](https://expressjs.com/) 服务器中使用 AI SDK，向客户端生成并流式传输文本与对象。

## 示例

以下示例会启动一个简单的 HTTP 服务器，监听 8080 端口。例如可使用 `curl` 测试：

```bash
curl -X POST http://localhost:8080
```

<Note>
  示例使用 Vercel AI Gateway。请确保已在 `AI_GATEWAY_API_KEY` 环境变量中设置 AI Gateway API 密钥。
</Note>

**完整示例**：[github.com/vercel/ai/examples/express](https://github.com/vercel/ai/tree/main/examples/express)

### UI Message Stream

你可以使用 `pipeUIMessageStreamToResponse` 方法将流数据管道到服务器响应。

```ts filename='index.ts'
import { streamText } from 'ai';
import express, { Request, Response } from 'express';

const app = express();

app.post('/', async (req: Request, res: Response) => {
  const result = streamText({
    model: 'openai/gpt-4o',
    prompt: 'Invent a new holiday and describe its traditions.',
  });

  result.pipeUIMessageStreamToResponse(res);
});

app.listen(8080, () => {
  console.log(`Example app listening on port ${8080}`);
});
```

### Sending Custom Data

`pipeUIMessageStreamToResponse` 可用于向客户端发送自定义数据。

```ts filename='index.ts'
import {
  createUIMessageStream,
  pipeUIMessageStreamToResponse,
  streamText,
} from 'ai';
import express, { Request, Response } from 'express';

const app = express();

app.post('/custom-data-parts', async (req: Request, res: Response) => {
  pipeUIMessageStreamToResponse({
    response: res,
    stream: createUIMessageStream({
      execute: async ({ writer }) => {
        writer.write({ type: 'start' });

        writer.write({
          type: 'data-custom',
          data: {
            custom: 'Hello, world!',
          },
        });

        const result = streamText({
          model: 'openai/gpt-4o',
          prompt: 'Invent a new holiday and describe its traditions.',
        });

        writer.merge(result.toUIMessageStream({ sendStart: false }));
      },
    }),
  });
});

app.listen(8080, () => {
  console.log(`Example app listening on port ${8080}`);
});
```

### Text Stream

你可以使用 `pipeTextStreamToResponse` 向客户端发送文本流。

```ts filename='index.ts'
import { streamText } from 'ai';
import express, { Request, Response } from 'express';

const app = express();

app.post('/', async (req: Request, res: Response) => {
  const result = streamText({
    model: 'openai/gpt-4o',
    prompt: 'Invent a new holiday and describe its traditions.',
  });

  result.pipeTextStreamToResponse(res);
});

app.listen(8080, () => {
  console.log(`Example app listening on port ${8080}`);
});
```

## 故障排除

- 在[代理](/docs/troubleshooting/streaming-not-working-when-proxied)环境下流式传输不工作
