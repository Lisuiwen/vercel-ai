---
title: 使用聊天提示生成文本
description: 学习如何使用 AI SDK 和 Node 通过聊天提示生成文本
tags: ['node', 'chat']
---

# 使用聊天提示生成文本

此前，我们已能通过单条消息提示、系统提示或两者组合来生成文本和对象。但有时你需要基于一系列消息生成文本。

聊天补全允许你基于一系列消息生成文本。这一系列消息可以是任意系统之间的任意交互，但最常见、最易理解的是用户与模型之间对话的消息序列。

```ts file='index.ts'
import { generateText } from 'ai';

const result = await generateText({
  model: 'openai/gpt-4o',
  maxOutputTokens: 1024,
  system: 'You are a helpful chatbot.',
  messages: [
    {
      role: 'user',
      content: [{ type: 'text', text: 'Hello!' }],
    },
    {
      role: 'assistant',
      content: [{ type: 'text', text: 'Hello! How can I help you today?' }],
    },
    {
      role: 'user',
      content: [{ type: 'text', text: 'I need help with my computer.' }],
    },
  ],
});

console.log(result.text);
```
