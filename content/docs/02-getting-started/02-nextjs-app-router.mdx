---
title: Next.js App Router 快速入门
description: 学习如何使用 AI SDK 与 Next.js App Router 构建你的第一个 agent。
---

# Next.js App Router 快速入门

AI SDK 是一款强大的 TypeScript 库，旨在帮助开发者构建 AI 驱动的应用。

在本快速入门教程中，你将搭建一个简单的 agent，并配有流式聊天的用户界面。过程中将掌握在自己的项目中使用 AI SDK 所需的核心概念与常用做法。

如果你还不熟悉 [Prompt Engineering](/docs/advanced/prompt-engineering) 与 [HTTP Streaming](/docs/foundations/streaming) 的概念，可以先阅读这些文档（可选）。

## 前置条件

要完成本快速入门，你需要：

- 在本地开发机上安装 Node.js 18+ 与 pnpm。
- 一个 [ Vercel AI Gateway ](https://vercel.com/ai-gateway) API key。

如果你还没有 Vercel AI Gateway 的 API key，可以在 Vercel 网站[注册](https://vercel.com/d?to=%2F%5Bteam%5D%2F%7E%2Fai&title=Go+to+AI+Gateway)后获取。

## 创建应用

首先创建一个新的 Next.js 应用。下面的命令会新建名为 `my-ai-app` 的目录，并在其中初始化一个基础的 Next.js 应用。

<div className="mb-4">
  <Note>
    在提示是否使用 App Router 和 Tailwind CSS 时，请务必选择 yes。
    如果你在找 Next.js Pages Router 的快速入门，可以
    在[这里](/docs/getting-started/nextjs-pages-router)查看。
  </Note>
</div>

<Snippet text="pnpm create next-app@latest my-ai-app" />

进入刚创建的目录：

<Snippet text="cd my-ai-app" />

### 安装依赖

安装 `ai` 与 `@ai-sdk/react`，即 AI 包以及 AI SDK 的 React hooks。AI SDK 的 [ Vercel AI Gateway provider ](/providers/ai-sdk-providers/ai-gateway) 已随 `ai` 包提供。你还需要安装 `zod`，这是用于定义 tool 输入的 schema 校验库。

<Note>
  本指南使用 Vercel AI Gateway provider，这样你可以用一把 API key 访问多家
  provider 的众多模型；你也可以安装对应包，切换到任意 provider 或模型。更多信息请查看可用的
  [AI SDK providers](/providers/ai-sdk-providers)。
</Note>

<div className="my-4">
  <Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
    <Tab>
      <Snippet text="pnpm add ai @ai-sdk/react zod" dark />
    </Tab>
    <Tab>
      <Snippet text="npm install ai @ai-sdk/react zod" dark />
    </Tab>
    <Tab>
      <Snippet text="yarn add ai @ai-sdk/react zod" dark />
    </Tab>

    <Tab>
      <Snippet text="bun add ai @ai-sdk/react zod" dark />
    </Tab>

  </Tabs>
</div>

### 配置 AI Gateway API key

在项目根目录创建 `.env.local`，并写入你的 AI Gateway API key。该 key 用于让你的应用通过身份验证访问 Vercel AI Gateway。

<Snippet text="touch .env.local" />

编辑 `.env.local` 文件：

```env filename=".env.local"
AI_GATEWAY_API_KEY=xxxxxxxxx
```

将 `xxxxxxxxx` 换成你真实的 Vercel AI Gateway API key。

<Note className="mb-4">
  AI SDK 的 Vercel AI Gateway Provider 默认会读取环境变量
  `AI_GATEWAY_API_KEY`。
</Note>

## 创建 Route Handler

创建一个 route handler：`app/api/chat/route.ts`，并加入以下代码：

```tsx filename="app/api/chat/route.ts"
import { streamText, UIMessage, convertToModelMessages } from 'ai';
__PROVIDER_IMPORT__;

export async function POST(req: Request) {
  const { messages }: { messages: UIMessage[] } = await req.json();

  const result = streamText({
    model: __MODEL__,
    messages: await convertToModelMessages(messages),
  });

  return result.toUIMessageStreamResponse();
}
```

这段代码在做什么：

1. 定义一个异步的 `POST` 请求处理函数，并从请求体中解构出 `messages`。`messages` 保存了你与聊天机器人之间的对话历史，为下一轮生成提供上下文。其类型为 `UIMessage`，面向应用 UI 设计——包含完整消息历史以及与时间戳等相关的元数据。
2. 调用 [`streamText`](/docs/reference/ai-sdk-core/stream-text)（从 `ai` 包导入）。该函数接收一个配置对象，内含 `model` provider 与第 1 步得到的 `messages`。你还可以传入更多 [settings](/docs/ai-sdk-core/settings) 以进一步定制模型行为。`messages` 字段需要 `ModelMessage[]`。它与 `UIMessage` 不同，不包含时间戳或发送者等元数据。要在两者之间转换，可使用 `convertToModelMessages`，它会去掉 UI 专有元数据，把 `UIMessage[]` 转成模型期望的 `ModelMessage[]`。
3. `streamText` 返回 [`StreamTextResult`](/docs/reference/ai-sdk-core/stream-text#result-object)。该结果对象包含 [ `toUIMessageStreamResponse` ](/docs/reference/ai-sdk-core/stream-text#to-ui-message-stream-response)，用于把结果转成可流式返回的响应对象。
4. 最后把结果返回给客户端，以流式输出响应。

该 Route Handler 在 `/api/chat` 上提供了一个 POST 端点。

## 选择 Provider

AI SDK 通过 [官方](/providers/ai-sdk-providers)、[OpenAI 兼容](/providers/openai-compatible-providers)与[社区](/providers/community-providers)包支持数十家 model provider。

本快速入门使用 [Vercel AI Gateway](https://vercel.com/ai-gateway) provider，它也是默认的 [global provider](/docs/ai-sdk-core/provider-management#global-provider-configuration)。因此你可以在 model 配置里用简单字符串引用模型：

```ts
model: __MODEL__;
```

你也可以用另外两种等价方式显式导入并使用 gateway provider：

```ts
// Option 1: Import from 'ai' package (included by default)
import { gateway } from 'ai';
model: gateway('anthropic/claude-sonnet-4.5');

// Option 2: Install and import from '@ai-sdk/gateway' package
import { gateway } from '@ai-sdk/gateway';
model: gateway('anthropic/claude-sonnet-4.5');
```

### 使用其他 provider

若要换用其他 provider，请安装对应包并创建 provider 实例。例如直接使用 OpenAI：

<div className="my-4">
  <Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
    <Tab>
      <Snippet text="pnpm add @ai-sdk/openai" dark />
    </Tab>
    <Tab>
      <Snippet text="npm install @ai-sdk/openai" dark />
    </Tab>
    <Tab>
      <Snippet text="yarn add @ai-sdk/openai" dark />
    </Tab>

    <Tab>
      <Snippet text="bun add @ai-sdk/openai" dark />
    </Tab>

  </Tabs>
</div>

```ts
import { openai } from '@ai-sdk/openai';

model: openai('gpt-5.1');
```

#### 更新 global provider

你可以修改默认的 global provider，让应用里所有以字符串形式书写的 model 都走你偏好的 provider。详情请阅读 [provider management](/docs/ai-sdk-core/provider-management#global-provider-configuration)。

请按你在全应用范围内管理 provider 的方式，选择最合适的一种做法。

## 连接前端

既然已经有了能查询 LLM 的 Route Handler，接下来配置前端。AI SDK 的 [ UI ](/docs/ai-sdk-ui) 包把聊天界面的复杂度收拢到一个 hook：[`useChat`](/docs/reference/ai-sdk-ui/use-chat)。

用下列代码更新根页面（`app/page.tsx`），以展示聊天消息列表并提供用户输入：

```tsx filename="app/page.tsx"
'use client';

import { useChat } from '@ai-sdk/react';
import { useState } from 'react';

export default function Chat() {
  const [input, setInput] = useState('');
  const { messages, sendMessage } = useChat();
  return (
    <div className="flex flex-col w-full max-w-md py-24 mx-auto stretch">
      {messages.map(message => (
        <div key={message.id} className="whitespace-pre-wrap">
          {message.role === 'user' ? 'User: ' : 'AI: '}
          {message.parts.map((part, i) => {
            switch (part.type) {
              case 'text':
                return <div key={`${message.id}-${i}`}>{part.text}</div>;
            }
          })}
        </div>
      ))}

      <form
        onSubmit={e => {
          e.preventDefault();
          sendMessage({ text: input });
          setInput('');
        }}
      >
        <input
          className="fixed dark:bg-zinc-900 bottom-0 w-full max-w-md p-2 mb-8 border border-zinc-300 dark:border-zinc-800 rounded shadow-xl"
          value={input}
          placeholder="Say something..."
          onChange={e => setInput(e.currentTarget.value)}
        />
      </form>
    </div>
  );
}
```

<Note>
  请确保在文件顶部添加 `"use client"` 指令，这样才能用 JavaScript 提供交互。
</Note>

本页面使用 `useChat` hook；默认情况下，它会请求你先前创建的 `POST` API route（`/api/chat`）。该 hook 提供处理用户输入与表单提交所需的函数与状态。`useChat` 还提供多种工具函数与状态，例如：

- `messages`：当前聊天消息（对象数组，含 `id`、`role`、`parts` 等字段）。
- `sendMessage`：向聊天 API 发送消息的函数。

组件用本地状态（`useState`）管理输入框内容；提交表单时调用 `sendMessage` 传入输入文本，然后清空输入。

LLM 的回复通过消息的 `parts` 数组访问。每条消息包含有序的 `parts`，表示模型在回复中生成的全部内容。这些 part 可以包含纯文本、reasoning tokens 等，后面你还会见到更多类型。`parts` 数组保留模型输出的顺序，便于你按生成顺序展示或处理每一段。

## 运行应用

至此，聊天机器人所需的后端与前端都已就绪。启动应用请执行：

<Snippet text="pnpm run dev" />

在浏览器中打开 http://localhost:3000。你会看到输入框，试着发送一条消息，观察 AI 聊天机器人如何实时回复。借助 AI SDK，用 Next.js 打造 AI 聊天界面既快又简单。

## 用 tool 增强聊天机器人

大语言模型（LLM）有很强的生成能力，但在离散任务（例如数学）以及与外部世界交互（例如查天气）方面往往吃力。这时就需要 [tools](/docs/ai-sdk-core/tools-and-tool-calling)。

tool 是 LLM 可以触发的操作；这些操作的结果可以回传给 LLM，供其在下一轮回复中参考。

例如用户问当前天气，没有 tool 时，模型只能根据训练数据给出泛泛信息；有了天气 tool，它就能拉取与地点相关的实时天气。

下面通过一个简单的天气 tool 来增强聊天机器人。

### 更新 Route Handler

修改 `app/api/chat/route.ts`，加入新的天气 tool：

```tsx filename="app/api/chat/route.ts" highlight="1,11-25"
import { streamText, UIMessage, convertToModelMessages, tool } from 'ai';
__PROVIDER_IMPORT__;
import { z } from 'zod';

export async function POST(req: Request) {
  const { messages }: { messages: UIMessage[] } = await req.json();

  const result = streamText({
    model: __MODEL__,
    messages: await convertToModelMessages(messages),
    tools: {
      weather: tool({
        description: 'Get the weather in a location (fahrenheit)',
        inputSchema: z.object({
          location: z.string().describe('The location to get the weather for'),
        }),
        execute: async ({ location }) => {
          const temperature = Math.round(Math.random() * (90 - 32) + 32);
          return {
            location,
            temperature,
          };
        },
      }),
    },
  });

  return result.toUIMessageStreamResponse();
}
```

本次更新的要点：

1. 从 `ai` 包导入 `tool`，从 `zod` 导入 `z`，用于 schema 校验。
2. 在 `tools` 里定义 `weather` tool，该 tool：
   - 带有 `description`，帮助模型判断何时调用。
   - 用 Zod schema 定义 `inputSchema`，要求提供字符串 `location` 才能执行。模型会尝试从对话上下文中抽取该参数；若无法抽取，会向用户追问缺失信息。
   - 定义 `execute`，这里用随机温度模拟天气数据（异步函数跑在服务端，你也可以改为请求真实外部 API）。

现在聊天机器人可以为用户询问的任意地点「获取」天气。当模型判断需要调用天气 tool 时，会生成带参数的 tool call；`execute` 会自动运行，tool 输出会以 `tool` 消息形式加入 `messages`。

试着问类似「What's the weather in New York?」，观察模型如何使用新 tool。

注意到 UI 里有时像是空白回复？那是因为模型没有生成普通文本，而是生成了 tool call。你可以在客户端通过 `message.parts` 里类型为 `tool-weather` 的 part 访问该 tool call 以及后续的 tool 结果。

<Note>
  Tool part 始终命名为 `tool-{toolName}`，其中 `{toolName}` 是你在定义 tool 时使用的键名。
  这里我们定义的 key 是 `weather`，因此 part 类型为 `tool-weather`。
</Note>

### 更新 UI

若要在界面中展示 tool 调用，请更新 `app/page.tsx`：

```tsx filename="app/page.tsx" highlight="18-22"
'use client';

import { useChat } from '@ai-sdk/react';
import { useState } from 'react';

export default function Chat() {
  const [input, setInput] = useState('');
  const { messages, sendMessage } = useChat();
  return (
    <div className="flex flex-col w-full max-w-md py-24 mx-auto stretch">
      {messages.map(message => (
        <div key={message.id} className="whitespace-pre-wrap">
          {message.role === 'user' ? 'User: ' : 'AI: '}
          {message.parts.map((part, i) => {
            switch (part.type) {
              case 'text':
                return <div key={`${message.id}-${i}`}>{part.text}</div>;
              case 'tool-weather':
                return (
                  <pre key={`${message.id}-${i}`}>
                    {JSON.stringify(part, null, 2)}
                  </pre>
                );
            }
          })}
        </div>
      ))}

      <form
        onSubmit={e => {
          e.preventDefault();
          sendMessage({ text: input });
          setInput('');
        }}
      >
        <input
          className="fixed dark:bg-zinc-900 bottom-0 w-full max-w-md p-2 mb-8 border border-zinc-300 dark:border-zinc-800 rounded shadow-xl"
          value={input}
          placeholder="Say something..."
          onChange={e => setInput(e.currentTarget.value)}
        />
      </form>
    </div>
  );
}
```

这样你便针对不同 message part 分支处理：文本 part 照旧展示文本；天气 tool 的调用则展示为 JSON，包含 tool call 与结果。

再次询问天气时，你会在聊天界面里看到 tool 调用及其返回。

## 启用多步 tool 调用

你可能已经发现：虽然 tool 在界面里可见，模型却没有用这些信息回答最初的问题。这是因为一旦模型生成了 tool call，从流程上讲它的这一轮生成已经结束。

要解决这一点，可以使用 `stopWhen` 启用多步 tool 调用。默认情况下，`stopWhen` 为 `isStepCount(1)`，表示出现 tool 结果时，第一步之后就停止生成。通过修改该条件，你可以让模型自动把 tool 结果喂回自身，触发后续生成，直到满足你设定的停止条件。这里我们希望模型在拿到天气 tool 的结果后继续生成，从而回答最初的问题。

### 更新 Route Handler

修改 `app/api/chat/route.ts`，加入 `stopWhen` 条件：

```tsx filename="app/api/chat/route.ts" highlight="16"
import {
  streamText,
  UIMessage,
  convertToModelMessages,
  tool,
  isStepCount,
} from 'ai';
__PROVIDER_IMPORT__;
import { z } from 'zod';

export async function POST(req: Request) {
  const { messages }: { messages: UIMessage[] } = await req.json();

  const result = streamText({
    model: __MODEL__,
    messages: await convertToModelMessages(messages),
    stopWhen: isStepCount(5),
    tools: {
      weather: tool({
        description: 'Get the weather in a location (fahrenheit)',
        inputSchema: z.object({
          location: z.string().describe('The location to get the weather for'),
        }),
        execute: async ({ location }) => {
          const temperature = Math.round(Math.random() * (90 - 32) + 32);
          return {
            location,
            temperature,
          };
        },
      }),
    },
    onStepFinish: ({ toolResults }) => {
      console.log(toolResults);
    },
  });

  return result.toUIMessageStreamResponse();
}
```

本次更新：

1. 将 `stopWhen` 设为 `isStepCount(5)`，允许单次对话最多进行 5 个「步骤」。
2. 增加 `onStepFinish` 回调，打印每一步的 `toolResults`，便于观察模型的 tool 使用方式。

回到浏览器，询问某地天气。现在模型应会利用天气 tool 的结果来回答你的问题。

设置 `stopWhen: isStepCount(5)` 后，模型在一次生成流程中最多可走 5 步，从而支持更复杂的交互，并在需要时分多步收集与处理信息。你还可以再加一个 tool，把温度从摄氏度转成华氏度，从行为上更直观地看到这一点。

### 再添加一个 tool

更新 `app/api/chat/route.ts`，新增将华氏温度转为摄氏温度的 tool：

```tsx filename="app/api/chat/route.ts" highlight="31-46"
import {
  streamText,
  UIMessage,
  convertToModelMessages,
  tool,
  isStepCount,
} from 'ai';
__PROVIDER_IMPORT__;
import { z } from 'zod';

export async function POST(req: Request) {
  const { messages }: { messages: UIMessage[] } = await req.json();

  const result = streamText({
    model: __MODEL__,
    messages: await convertToModelMessages(messages),
    stopWhen: isStepCount(5),
    tools: {
      weather: tool({
        description: 'Get the weather in a location (fahrenheit)',
        inputSchema: z.object({
          location: z.string().describe('The location to get the weather for'),
        }),
        execute: async ({ location }) => {
          const temperature = Math.round(Math.random() * (90 - 32) + 32);
          return {
            location,
            temperature,
          };
        },
      }),
      convertFahrenheitToCelsius: tool({
        description: 'Convert a temperature in fahrenheit to celsius',
        inputSchema: z.object({
          temperature: z
            .number()
            .describe('The temperature in fahrenheit to convert'),
        }),
        execute: async ({ temperature }) => {
          const celsius = Math.round((temperature - 32) * (5 / 9));
          return {
            celsius,
          };
        },
      }),
    },
  });

  return result.toUIMessageStreamResponse();
}
```

### 更新前端

更新 `app/page.tsx`，渲染新的温度转换 tool：

```tsx filename="app/page.tsx" highlight="19"
'use client';

import { useChat } from '@ai-sdk/react';
import { useState } from 'react';

export default function Chat() {
  const [input, setInput] = useState('');
  const { messages, sendMessage } = useChat();
  return (
    <div className="flex flex-col w-full max-w-md py-24 mx-auto stretch">
      {messages.map(message => (
        <div key={message.id} className="whitespace-pre-wrap">
          {message.role === 'user' ? 'User: ' : 'AI: '}
          {message.parts.map((part, i) => {
            switch (part.type) {
              case 'text':
                return <div key={`${message.id}-${i}`}>{part.text}</div>;
              case 'tool-weather':
              case 'tool-convertFahrenheitToCelsius':
                return (
                  <pre key={`${message.id}-${i}`}>
                    {JSON.stringify(part, null, 2)}
                  </pre>
                );
            }
          })}
        </div>
      ))}

      <form
        onSubmit={e => {
          e.preventDefault();
          sendMessage({ text: input });
          setInput('');
        }}
      >
        <input
          className="fixed dark:bg-zinc-900 bottom-0 w-full max-w-md p-2 mb-8 border border-zinc-300 dark:border-zinc-800 rounded shadow-xl"
          value={input}
          placeholder="Say something..."
          onChange={e => setInput(e.currentTarget.value)}
        />
      </form>
    </div>
  );
}
```

此次更新会处理新的 `tool-convertFahrenheitToCelsius` part 类型，在 UI 中展示温度转换 tool 的调用与结果。

当你问「What's the weather in New York in celsius?」时，应能看到更完整的一条链路：

1. 模型会先为纽约调用天气 tool。
2. 你会看到 tool 输出展示在界面上。
3. 随后它会调用温度转换 tool，把华氏温度转为摄氏温度。
4. 最后模型会综合这些信息，用自然语言说明纽约的天气。

这种多步方式让模型能先收集信息再作答，回复更准确、更贴合上下文，聊天机器人也会实用得多。

这个简单示例展示了 tool 如何扩展模型能力。你还可以接入真实 API、数据库或其他外部系统，构建更复杂的 tool，让模型实时访问并处理真实世界的数据，弥合模型知识截止与当前信息之间的鸿沟。

## 下一步做什么？

你已经在 AI SDK 上搭好了一个 AI 聊天机器人。接下来可以：

- 想深入了解 AI SDK，请阅读[文档](/docs)。
- 若希望跟着指南深入实践，可看 [RAG（检索增强生成）](/cookbook/guides/rag-chatbot)与[多模态聊天机器人](/cookbook/guides/multi-modal-chatbot)教程。
- 若想快速启动第一个 AI 项目，可浏览可用的[模板](https://vercel.com/templates?type=ai)。
