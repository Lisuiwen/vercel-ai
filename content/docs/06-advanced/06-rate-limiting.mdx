---
title: 速率限制
description: 学习如何为应用设置速率限制。
---

# 速率限制

速率限制有助于保护 API 免受滥用。它通过在指定时间窗口内限制客户端可发起的最大请求数来实现。这一简单技术充当「守门人」，防止过度使用导致服务性能下降和不必要的成本。

## 使用 Vercel KV 和 Upstash Ratelimit 进行速率限制

本示例将使用 [Vercel KV](https://vercel.com/storage/kv) 和 [Upstash Ratelimit](https://github.com/upstash/ratelimit) 保护 API 端点。

```tsx filename='app/api/generate/route.ts'
import kv from '@vercel/kv';
import { streamText } from 'ai';
__PROVIDER_IMPORT__;
import { Ratelimit } from '@upstash/ratelimit';
import { NextRequest } from 'next/server';

// Allow streaming responses up to 30 seconds
export const maxDuration = 30;

// Create Rate limit
const ratelimit = new Ratelimit({
  redis: kv,
  limiter: Ratelimit.fixedWindow(5, '30s'),
});

export async function POST(req: NextRequest) {
  // call ratelimit with request ip
  const ip = req.ip ?? 'ip';
  const { success, remaining } = await ratelimit.limit(ip);

  // block the request if unsuccessful
  if (!success) {
    return new Response('Ratelimited!', { status: 429 });
  }

  const { messages } = await req.json();

  const result = streamText({
    model: __MODEL__,
    messages,
  });

  return result.toUIMessageStreamResponse();
}
```

## 简化 API 保护

借助 Vercel KV 和 Upstash Ratelimit，可以轻松保护 API 免受此类攻击。要了解 Ratelimit 的工作原理以及如何按需配置，请参阅 [Ratelimit 文档](https://upstash.com/docs/oss/sdks/ts/ratelimit/overview)。
