---
title: cosineSimilarity
description: 计算两个向量之间的余弦相似度（API 参考）
---

# `cosineSimilarity()`

比较嵌入向量相似度时，通常使用余弦相似度等标准向量度量。

`cosineSimilarity` 计算两个向量之间的余弦相似度。
较高值（接近 1）表示向量很相似，较低值（接近 -1）表示差异较大。

```ts
import { cosineSimilarity, embedMany } from 'ai';

const { embeddings } = await embedMany({
  model: 'openai/text-embedding-3-small',
  values: ['sunny day at the beach', 'rainy afternoon in the city'],
});

console.log(
  `cosine similarity: ${cosineSimilarity(embeddings[0], embeddings[1])}`,
);
```

## 导入

<Snippet text={`import { cosineSimilarity } from "ai"`} prompt={false} />

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'vector1',
      type: 'number[]',
      description: '要比较的第一个向量',
    },
    {
      name: 'vector2',
      type: 'number[]',
      description: '要比较的第二个向量',
    },
  ]}
/>

### 返回值

介于 -1 与 1 之间的数，表示两向量的余弦相似度。
