---
title: uploadFile
description: uploadFile 的 API 参考。
---

# `uploadFile()`

将文件上传到提供商并返回 `ProviderReference`，可在后续 API 调用中使用，例如作为传给 `generateText` 或 `streamText` 的消息内容部分。

```ts
import { uploadFile } from 'ai';
import { openai } from '@ai-sdk/openai';
import fs from 'node:fs';

const { providerReference } = await uploadFile({
  api: openai.files(),
  data: fs.readFileSync('./photo.png'),
  filename: 'photo.png',
});
```

## 导入

<Snippet text={`import { uploadFile } from "ai"`} prompt={false} />

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'api',
      type: 'FilesV4 | ProviderV4',
      description:
        '用于上传的文件 API 接口。可为 `FilesV4` 实例（如 `openai.files()`），或直接传入提供商实例（如 `openai`），此时会自动调用 `.files()`。',
    },
    {
      name: 'data',
      type: 'DataContent',
      description:
        '要上传的文件数据。可为 `Uint8Array`、base64 字符串、`ArrayBuffer` 或 `Buffer`。不支持 URL — 请先拉取内容再传入字节。',
    },
    {
      name: 'mediaType',
      type: 'string',
      isOptional: true,
      description:
        '文件的 IANA 媒体类型（如 `image/png`、`application/pdf`）。未提供时会从文件字节自动检测。',
    },
    {
      name: 'filename',
      type: 'string',
      isOptional: true,
      description: '上传文件的文件名。',
    },
    {
      name: 'providerOptions',
      type: 'ProviderOptions',
      isOptional: true,
      description:
        '额外的提供商专用选项。例如 OpenAI 需要 `purpose` 字段。',
    },
  ]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'providerReference',
      type: 'ProviderReference',
      description:
        '将提供商名称映射到提供商文件标识的 `Record<string, string>`。可作为消息内容部分的 `data` 或 `image` 字段传入。',
    },
    {
      name: 'providerMetadata',
      type: 'ProviderMetadata',
      isOptional: true,
      description:
        '上传返回的额外提供商专用元数据。',
    },
    {
      name: 'warnings',
      type: 'Warning[]',
      description: '提供商的警告（例如不支持的设置）。',
    },
  ]}
/>
