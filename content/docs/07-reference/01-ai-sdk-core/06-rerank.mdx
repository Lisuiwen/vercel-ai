---
title: rerank
description: rerank 的 API 参考。
---

# `rerank()`

使用重排模型，根据一组文档与查询的相关性对其重新排序。

这非常适合提升搜索相关性：基于对查询和文档的语义理解，对文档、邮件或其他内容重新排序。

```ts
import { cohere } from '@ai-sdk/cohere';
import { rerank } from 'ai';

const { ranking } = await rerank({
  model: cohere.reranking('rerank-v3.5'),
  documents: ['sunny day at the beach', 'rainy afternoon in the city'],
  query: 'talk about rain',
});
```

## 导入

<Snippet text={`import { rerank } from "ai"`} prompt={false} />

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'model',
      type: 'RerankingModel',
      description:
        "要使用的重排模型。示例：cohere.reranking('rerank-v3.5')",
    },
    {
      name: 'documents',
      type: 'Array<VALUE>',
      description:
        '要重排的文档。可以是字符串数组或 JSON 对象数组。',
    },
    {
      name: 'query',
      type: 'string',
      description: '用于对文档排序的搜索查询。',
    },
    {
      name: 'topN',
      type: 'number',
      isOptional: true,
      description:
        '要返回的顶部文档最大数量。如果未指定，则返回所有文档。',
    },
    {
      name: 'maxRetries',
      type: 'number',
      isOptional: true,
      description:
        '最大重试次数。设置为 0 可禁用重试。默认值：2。',
    },
    {
      name: 'abortSignal',
      type: 'AbortSignal',
      isOptional: true,
      description:
        '可选的中止信号，可用于取消调用。',
    },
    {
      name: 'headers',
      type: 'Record<string, string>',
      isOptional: true,
      description:
        '随请求发送的额外 HTTP 标头。仅适用于基于 HTTP 的 provider。',
    },
    {
      name: 'providerOptions',
      type: 'ProviderOptions',
      isOptional: true,
      description: '重排请求的 provider 专属选项。',
    },
    {
      name: 'telemetry',
      type: 'TelemetryOptions',
      isOptional: true,
      description: '遥测配置。',
      properties: [
        {
          type: 'TelemetryOptions',
          parameters: [
            {
              name: 'isEnabled',
              type: 'boolean',
              isOptional: true,
              description:
                '启用或禁用遥测。默认启用。设置为 `false` 可选择退出。',
            },
            {
              name: 'recordInputs',
              type: 'boolean',
              isOptional: true,
              description:
                '启用或禁用输入记录。默认启用。',
            },
            {
              name: 'recordOutputs',
              type: 'boolean',
              isOptional: true,
              description:
                '启用或禁用输出记录。默认启用。',
            },
            {
              name: 'functionId',
              type: 'string',
              isOptional: true,
              description:
                '此函数的标识符。用于按函数分组遥测数据。',
            },
            {
              name: 'integrations',
              isOptional: true,
              type: 'Telemetry | Telemetry[]',
              description:
                '接收生命周期事件的单次调用遥测集成。提供后，它们会替换此次调用中任何全局注册的集成。',
            },
          ],
        },
      ],
    },
    {
      name: 'experimental_onStart',
      type: '(event: RerankStartEvent) => void | Promise<void>',
      isOptional: true,
      description:
        '在 rerank 操作开始、调用重排模型之前调用。',
    },
    {
      name: 'experimental_onEnd',
      type: '(event: RerankEndEvent) => void | Promise<void>',
      isOptional: true,
      description:
        '在 rerank 操作完成、重排模型返回之后调用。',
    },
  ]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'originalDocuments',
      type: 'Array<VALUE>',
      description: '按原始顺序排列的原始文档数组。',
    },
    {
      name: 'rerankedDocuments',
      type: 'Array<VALUE>',
      description: '按相关性分数排序的文档（降序）。',
    },
    {
      name: 'ranking',
      type: 'Array<RankingItem<VALUE>>',
      description: '包含分数和索引的排名项数组。',
      properties: [
        {
          type: 'RankingItem<VALUE>',
          parameters: [
            {
              name: 'originalIndex',
              type: 'number',
              description:
                '文档在原始文档数组中的索引。',
            },
            {
              name: 'score',
              type: 'number',
              description:
                '文档的相关性分数（通常为 0-1，越高表示越相关）。',
            },
            {
              name: 'document',
              type: 'VALUE',
              description: '文档本身。',
            },
          ],
        },
      ],
    },
    {
      name: 'response',
      type: 'Response',
      description: '响应数据。',
      properties: [
        {
          type: 'Response',
          parameters: [
            {
              name: 'id',
              isOptional: true,
              type: 'string',
              description: 'provider 返回的响应 ID。',
            },
            {
              name: 'timestamp',
              type: 'Date',
              description: '响应的时间戳。',
            },
            {
              name: 'modelId',
              type: 'string',
              description: '用于重排的模型 ID。',
            },
            {
              name: 'headers',
              isOptional: true,
              type: 'Record<string, string>',
              description: '响应标头。',
            },
            {
              name: 'body',
              type: 'unknown',
              isOptional: true,
              description: '原始响应正文。',
            },
          ],
        },
      ],
    },
    {
      name: 'providerMetadata',
      type: 'ProviderMetadata | undefined',
      isOptional: true,
      description:
        '来自 provider 的可选元数据。外层键是 provider 名称，内层值是元数据。具体细节取决于 provider。',
    },
  ]}
/>

## 示例

### 字符串文档

```ts
import { cohere } from '@ai-sdk/cohere';
import { rerank } from 'ai';

const { ranking, rerankedDocuments } = await rerank({
  model: cohere.reranking('rerank-v3.5'),
  documents: [
    'sunny day at the beach',
    'rainy afternoon in the city',
    'snowy night in the mountains',
  ],
  query: 'talk about rain',
  topN: 2,
});

console.log(rerankedDocuments);
// ['rainy afternoon in the city', 'sunny day at the beach']

console.log(ranking);
// [
//   { originalIndex: 1, score: 0.9, document: 'rainy afternoon...' },
//   { originalIndex: 0, score: 0.3, document: 'sunny day...' }
// ]
```

### 对象文档

```ts
import { cohere } from '@ai-sdk/cohere';
import { rerank } from 'ai';

const documents = [
  {
    from: 'Paul Doe',
    subject: 'Follow-up',
    text: 'We are happy to give you a discount of 20%.',
  },
  {
    from: 'John McGill',
    subject: 'Missing Info',
    text: 'Here is the pricing from Oracle: $5000/month',
  },
];

const { ranking } = await rerank({
  model: cohere.reranking('rerank-v3.5'),
  documents,
  query: 'Which pricing did we get from Oracle?',
  topN: 1,
});

console.log(ranking[0].document);
// { from: 'John McGill', subject: 'Missing Info', ... }
```

### 使用 Provider Options

```ts
import { cohere } from '@ai-sdk/cohere';
import { rerank } from 'ai';

const { ranking } = await rerank({
  model: cohere.reranking('rerank-v3.5'),
  documents: ['sunny day at the beach', 'rainy afternoon in the city'],
  query: 'talk about rain',
  providerOptions: {
    cohere: {
      maxTokensPerDoc: 1000,
    },
  },
});
```
