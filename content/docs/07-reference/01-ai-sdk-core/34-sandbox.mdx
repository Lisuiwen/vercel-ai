---
title: Experimental_Sandbox
description: Experimental_Sandbox 接口的 API 参考。
---

# `Experimental_Sandbox`

`Experimental_Sandbox` 接口描述工具可用于运行命令的执行环境。通过 `experimental_sandbox` 选项传入实验性沙箱至 `generateText`、`streamText`、`ToolLoopAgent.generate`、`ToolLoopAgent.stream` 或智能体 UI 流辅助函数，即可在工具描述函数与工具执行中使用。

<Note type="warning">
  该 API 为实验性，可能在补丁版本中变更。传入实验性沙箱**并不会**将工具本身沙箱化；除非工具显式将工作委托给实验性沙箱，否则工具代码仍在应用进程中运行。
</Note>

## 导入

<Snippet
  text={`import type { Experimental_Sandbox } from "ai"`}
  prompt={false}
/>

## 类型定义

```ts
type Experimental_Sandbox = {
  readonly description: string;
  readonly runCommand: (options: {
    command: string;
    workingDirectory?: string;
    abortSignal?: AbortSignal;
  }) => PromiseLike<{
    exitCode: number;
    stdout: string;
    stderr: string;
  }>;
};
```

## 属性

<PropertiesTable
  content={[
    {
      name: 'description',
      type: 'string',
      description:
        '实验性沙箱环境的描述。当模型需要了解根目录、开放端口、公共主机名或其他环境约束时，请将其包含在提示或说明中。AI SDK 不会自动将其加入提示。',
    },
    {
      name: 'runCommand',
      type: '(options: { command: string; workingDirectory?: string; abortSignal?: AbortSignal }) => PromiseLike<{ exitCode: number; stdout: string; stderr: string }>',
      description:
        '在实验性沙箱中执行命令，并解析为退出码、标准输出与标准错误。',
      properties: [
        {
          type: 'options',
          parameters: [
            {
              name: 'command',
              type: 'string',
              description:
                '在实验性沙箱中执行的命令。',
            },
            {
              name: 'workingDirectory',
              type: 'string | undefined',
              description:
                '执行命令的可选工作目录。若省略，实验性沙箱实现使用其默认工作目录。',
            },
            {
              name: 'abortSignal',
              type: 'AbortSignal | undefined',
              description:
                '实验性沙箱实现可用于取消命令的可选中止信号。',
            },
          ],
        },
      ],
    },
  ]}
/>

## 示例

```ts
const result = await generateText({
  model: __MODEL__,
  tools: { shell },
  experimental_sandbox,
  prompt: 'Run the test suite.',
});
```

在工具内，从 `execute` 的第二个参数读取实验性沙箱：

```ts
const shell = tool({
  inputSchema: z.object({
    command: z.string(),
    workingDirectory: z.string().optional(),
  }),
  execute: async (
    { command, workingDirectory },
    { abortSignal, experimental_sandbox },
  ) => {
    if (!experimental_sandbox) {
      throw new Error('Experimental sandbox is not available');
    }

    return experimental_sandbox.runCommand({
      command,
      workingDirectory,
      abortSignal,
    });
  },
});
```

## 另请参阅

- [工具调用：实验性沙箱](/docs/ai-sdk-core/tools-and-tool-calling#experimental-sandbox)
- [`generateText`](/docs/reference/ai-sdk-core/generate-text)
- [`streamText`](/docs/reference/ai-sdk-core/stream-text)
- [`ToolLoopAgent`](/docs/reference/ai-sdk-core/tool-loop-agent)
