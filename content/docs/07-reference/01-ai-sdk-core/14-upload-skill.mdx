---
title: uploadSkill
description: uploadSkill 的 API 参考。
---

# `uploadSkill()`

将技能（文件包）上传到提供商并返回 `ProviderReference`，可在后续推理调用中引用。

```ts
import { uploadSkill } from 'ai';
import { anthropic } from '@ai-sdk/anthropic';
import { readFileSync } from 'fs';

const { providerReference } = await uploadSkill({
  api: anthropic.skills(),
  files: [
    {
      path: 'my-skill/SKILL.md',
      content: readFileSync('./SKILL.md'),
    },
  ],
  displayTitle: 'My Skill',
});
```

## 导入

<Snippet text={`import { uploadSkill } from "ai"`} prompt={false} />

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'api',
      type: 'SkillsV4 | ProviderV4',
      description:
        '用于上传的技能 API 接口。可为 `SkillsV4` 实例（如 `anthropic.skills()`），或直接传入提供商实例（如 `anthropic`），此时会自动调用 `.skills()`。',
    },
    {
      name: 'files',
      type: 'SkillsV4File[]',
      description:
        '构成技能的文件列表。每项包含相对路径 `path` 与 `content`（`Uint8Array` 或 base64 字符串）。',
    },
    {
      name: 'displayTitle',
      type: 'string',
      isOptional: true,
      description: '人类可读的技能标题。',
    },
    {
      name: 'providerOptions',
      type: 'ProviderOptions',
      isOptional: true,
      description: '额外的提供商专用选项。',
    },
  ]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'providerReference',
      type: 'ProviderReference',
      description:
        '将提供商名称映射到提供商技能标识的 `Record<string, string>`。在推理中引用技能时传入。',
    },
    {
      name: 'displayTitle',
      type: 'string',
      isOptional: true,
      description:
        '提供商返回的人类可读标题（若支持）。',
    },
    {
      name: 'name',
      type: 'string',
      isOptional: true,
      description:
        '提供商根据上传文件推断的技能名称。',
    },
    {
      name: 'description',
      type: 'string',
      isOptional: true,
      description:
        '提供商根据上传文件推断的技能描述。',
    },
    {
      name: 'latestVersion',
      type: 'string',
      isOptional: true,
      description: '提供商分配的最新版本标识。',
    },
    {
      name: 'providerMetadata',
      type: 'ProviderMetadata',
      isOptional: true,
      description:
        '上传返回的额外提供商专用元数据。',
    },
    {
      name: 'warnings',
      type: 'Warning[]',
      description: '提供商的警告（例如不支持的设置）。',
    },
  ]}
/>
