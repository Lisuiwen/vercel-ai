---
title: transcribe
description: transcribe 的 API 参考。
---

# `transcribe()`

<Note type="warning">`transcribe` 为实验性功能。</Note>

从音频文件生成文字稿。

```ts
import { experimental_transcribe as transcribe } from 'ai';
import { openai } from '@ai-sdk/openai';
import { readFile } from 'fs/promises';

const { text: transcript } = await transcribe({
  model: openai.transcription('whisper-1'),
  audio: await readFile('audio.mp3'),
});

console.log(transcript);
```

## 导入

<Snippet
  text={`import { experimental_transcribe as transcribe } from "ai"`}
  prompt={false}
/>

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'model',
      type: 'TranscriptionModelV4',
      description: '要使用的转录模型。',
    },
    {
      name: 'audio',
      type: 'DataContent (string | Uint8Array | ArrayBuffer | Buffer) | URL',
      description: '用于生成文字稿的音频文件。',
    },
    {
      name: 'providerOptions',
      type: 'Record<string, JSONObject>',
      isOptional: true,
      description: '额外的提供商专用选项。',
    },
    {
      name: 'maxRetries',
      type: 'number',
      isOptional: true,
      description: '最大重试次数。默认：2。',
    },
    {
      name: 'abortSignal',
      type: 'AbortSignal',
      isOptional: true,
      description: '可选的取消信号。',
    },
    {
      name: 'headers',
      type: 'Record<string, string>',
      isOptional: true,
      description: '请求的额外 HTTP 头。',
    },
    {
      name: 'download',
      type: '(options: { url: URL; abortSignal?: AbortSignal }) => Promise<{ data: Uint8Array; mediaType: string | undefined }>',
      isOptional: true,
      description:
        '从 URL 获取音频的自定义下载函数。可使用 `ai` 的 `createDownload()` 创建带自定义大小限制的下载函数，例如 `createDownload({ maxBytes: 50 * 1024 * 1024 })`。默认：内置下载，上限 2 GiB。',
    },
  ]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'text',
      type: 'string',
      description: '来自音频输入的完整转录文本。',
    },
    {
      name: 'segments',
      type: 'Array<{ text: string; startSecond: number; endSecond: number }>',
      description:
        '转录片段数组，每段包含部分文本及起止时间（秒）。',
    },
    {
      name: 'language',
      type: 'string | undefined',
      description:
        '转录语言，ISO-639-1，例如英语的 "en"。',
    },
    {
      name: 'durationInSeconds',
      type: 'number | undefined',
      description: '转录时长（秒）。',
    },
    {
      name: 'warnings',
      type: 'Warning[]',
      description:
        '模型提供商的警告（例如不支持的设置）。',
    },
    {
      name: 'providerMetadata',
      type: 'Record<string, JSONObject>',
      isOptional: true,
      description:
        '提供商返回的可选元数据。外层键为提供商名，内层为具体元数据，细节因提供商而异。',
    },
    {
      name: 'responses',
      type: 'Array<TranscriptionModelResponseMetadata>',
      description:
        '提供商的响应元数据。若对模型发起多次调用则可能有多个条目。',
      properties: [
        {
          type: 'TranscriptionModelResponseMetadata',
          parameters: [
            {
              name: 'timestamp',
              type: 'Date',
              description: '生成响应开始时刻的时间戳。',
            },
            {
              name: 'modelId',
              type: 'string',
              description:
                '用于生成响应的模型 ID。',
            },
            {
              name: 'headers',
              type: 'Record<string, string>',
              isOptional: true,
              description: '响应头。',
            },
          ],
        },
      ],
    },
  ]}
/>
