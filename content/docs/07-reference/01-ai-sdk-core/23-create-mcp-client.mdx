---
title: createMCPClient
description: 创建用于连接 MCP 服务器的客户端
---

# `createMCPClient()`

创建一个轻量级 Model Context Protocol (MCP) 客户端，用于连接 MCP 服务器。该客户端提供：

- **Tools**：在 MCP 工具与 AI SDK 工具之间自动转换
- **Resources**：用于列出、读取和发现 MCP 服务器资源模板的方法
- **Prompts**：用于列出可用提示和检索提示消息的方法
- **Elicitation**：支持在工具执行期间处理服务器对额外输入的请求

它目前不支持接收来自 MCP 服务器的通知，也不支持客户端的自定义配置。

## 导入

<Snippet
  text={`import { createMCPClient } from "@ai-sdk/mcp"`}
  prompt={false}
/>

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'config',
      type: 'MCPClientConfig',
      description: 'MCP 客户端的配置。',
      properties: [
        {
          type: 'MCPClientConfig',
          parameters: [
            {
              name: 'transport',
              type: 'MCPTransportConfig | MCPTransport',
              description: '消息传输层的配置。',
              properties: [
                {
                  type: 'MCPTransport',
                  description:
                    '客户端传输实例，显式用于 stdio 或自定义传输',
                  parameters: [
                    {
                      name: 'start',
                      type: '() => Promise<void>',
                      description: '启动传输的方法',
                    },
                    {
                      name: 'send',
                      type: '(message: JSONRPCMessage) => Promise<void>',
                      description:
                        '通过传输发送消息的方法',
                    },
                    {
                      name: 'close',
                      type: '() => Promise<void>',
                      description: '关闭传输的方法',
                    },
                    {
                      name: 'onclose',
                      type: '() => void',
                      description:
                        '传输关闭时调用的方法',
                    },
                    {
                      name: 'onerror',
                      type: '(error: Error) => void',
                      description:
                        '传输遇到错误时调用的方法',
                    },
                    {
                      name: 'onmessage',
                      type: '(message: JSONRPCMessage) => void',
                      description:
                        '传输收到消息时调用的方法',
                    },
                  ],
                },
                {
                  type: 'MCPTransportConfig',
                  parameters: [
                    {
                      name: 'type',
                      type: "'sse' | 'http",
                      description: '使用 Server-Sent Events 进行通信',
                    },
                    {
                      name: 'url',
                      type: 'string',
                      description: 'MCP 服务器的 URL',
                    },
                    {
                      name: 'headers',
                      type: 'Record<string, string>',
                      isOptional: true,
                      description:
                        '随请求发送的附加 HTTP 标头。',
                    },
                    {
                      name: 'authProvider',
                      type: 'OAuthClientProvider',
                      isOptional: true,
                      description:
                        '可选的 OAuth provider，用于授权访问受保护的远程 MCP 服务器。',
                    },
                    {
                      name: 'redirect',
                      type: "'follow' | 'error'",
                      isOptional: true,
                      description:
                        "控制传输请求如何处理 HTTP 重定向。设置为 'follow' 可允许重定向响应。默认为 'error'，用于拒绝任何重定向响应，防止服务器将请求重定向到非预期主机。",
                    },
                  ],
                },
              ],
            },
            {
              name: 'clientName',
              type: 'string',
              isOptional: true,
              description: '客户端名称。默认为 "ai-sdk-mcp-client"。',
            },
            {
              name: 'name',
              type: 'string',
              isOptional: true,
              description:
                '已弃用。请改用 `clientName`。默认为 "ai-sdk-mcp-client"。',
            },
            {
              name: 'version',
              type: 'string',
              isOptional: true,
              description: '客户端版本。默认为 "1.0.0"',
            },
            {
              name: 'onUncaughtError',
              type: '(error: unknown) => void',
              isOptional: true,
              description: '未捕获错误的处理程序',
            },
            {
              name: 'capabilities',
              type: 'ClientCapabilities',
              isOptional: true,
              description:
                '初始化期间要声明的可选客户端能力。例如，设置 { elicitation: {} } 可启用对服务器 elicitation 请求的处理。',
            },
          ],
        },
      ],
    },
  ]}
/>

### 返回值

返回一个 Promise，该 Promise 会解析为具有以下属性和方法的 `MCPClient`：

<PropertiesTable
  content={[
    {
      name: 'serverInfo',
      type: 'Configuration',
      description:
        '已连接 MCP 服务器的信息（名称、版本、可选标题），由初始化握手期间报告。',
    },
    {
      name: 'instructions',
      type: 'string',
      isOptional: true,
      description:
        '服务器在初始化握手期间提供的可选说明。描述如何使用服务器及其功能。适合包含在 LLM system prompt 中。',
    },
    {
      name: 'tools',
      type: `async (options?: {
        schemas?: TOOL_SCHEMAS
      }) => Promise<McpToolSet<TOOL_SCHEMAS>>`,
      description: '获取 MCP 服务器提供的工具。',
      properties: [
        {
          type: 'options',
          parameters: [
            {
              name: 'schemas',
              type: 'TOOL_SCHEMAS',
              isOptional: true,
              description:
                '用于编译时类型检查的 schema 定义。未提供时，将从服务器推断 schema。每个工具 schema 可以包含用于类型化输入的 inputSchema；当服务器返回 structuredContent 时，也可以包含用于类型化输出的可选 outputSchema。',
            },
          ],
        },
        {
          type: 'TOOL_SCHEMAS',
          parameters: [
            {
              name: 'inputSchema',
              type: 'FlexibleSchema',
              description:
                '定义工具预期输入参数的 Zod schema 或 JSON schema。',
            },
            {
              name: 'outputSchema',
              type: 'FlexibleSchema',
              isOptional: true,
              description:
                '定义预期输出结构的 Zod schema 或 JSON schema。提供后，客户端会从工具结果中提取并验证 structuredContent，为你提供类型化输出。',
            },
          ],
        },
      ],
    },
    {
      name: 'listTools',
      type: `async (options?: {
        params?: PaginatedRequest['params'];
        options?: RequestOptions;
      }) => Promise<ListToolsResult>`,
      description:
        '列出 MCP 服务器提供的工具定义，而不将其转换为 AI SDK 工具。',
      properties: [
        {
          type: 'options',
          parameters: [
            {
              name: 'params',
              type: "PaginatedRequest['params']",
              isOptional: true,
              description: '可选分页参数，包括 cursor。',
            },
            {
              name: 'options',
              type: 'RequestOptions',
              isOptional: true,
              description:
                '可选请求选项，包括 signal 和 timeout。',
            },
          ],
        },
      ],
    },
    {
      name: 'callTool',
      type: `async (args: {
        name: string;
        arguments?: Record<string, unknown>;
        options?: RequestOptions;
      }) => Promise<CallToolResult>`,
      description:
        '调用 MCP 服务器上的工具。这适用于由 host 中介的调用，例如 MCP Apps iframe 请求。',
      properties: [
        {
          type: 'args',
          parameters: [
            {
              name: 'name',
              type: 'string',
              description: '要调用的 MCP 工具名称。',
            },
            {
              name: 'arguments',
              type: 'Record<string, unknown>',
              isOptional: true,
              description: '传递给 MCP 工具的 arguments。',
            },
            {
              name: 'options',
              type: 'RequestOptions',
              isOptional: true,
              description:
                '可选请求选项，包括 signal 和 timeout。',
            },
          ],
        },
      ],
    },
    {
      name: 'toolsFromDefinitions',
      type: `(definitions: ListToolsResult, options?: {
        schemas?: TOOL_SCHEMAS
      }) => McpToolSet<TOOL_SCHEMAS>`,
      description:
        '将现有 MCP 工具定义转换为 AI SDK 工具，而无需再次列出工具。',
      properties: [
        {
          type: 'parameters',
          parameters: [
            {
              name: 'definitions',
              type: 'ListToolsResult',
              description:
                '工具定义，通常由 `listTools` 返回。',
            },
            {
              name: 'schemas',
              type: 'TOOL_SCHEMAS',
              isOptional: true,
              description:
                '用于编译时类型检查和类型化输出的可选 schema 定义。',
            },
          ],
        },
      ],
    },
    {
      name: 'listResources',
      type: `async (options?: {
        params?: PaginatedRequest['params'];
        options?: RequestOptions;
      }) => Promise<ListResourcesResult>`,
      description: '列出 MCP 服务器提供的所有资源。',
      properties: [
        {
          type: 'options',
          parameters: [
            {
              name: 'params',
              type: "PaginatedRequest['params']",
              isOptional: true,
              description: '可选分页参数，包括 cursor。',
            },
            {
              name: 'options',
              type: 'RequestOptions',
              isOptional: true,
              description:
                '可选请求选项，包括 signal 和 timeout。',
            },
          ],
        },
      ],
    },
    {
      name: 'readResource',
      type: `async (args: {
        uri: string;
        options?: RequestOptions;
      }) => Promise<ReadResourceResult>`,
      description: '按 URI 读取特定资源的内容。',
      properties: [
        {
          type: 'args',
          parameters: [
            {
              name: 'uri',
              type: 'string',
              description: '要读取的资源的 URI。',
            },
            {
              name: 'options',
              type: 'RequestOptions',
              isOptional: true,
              description:
                '可选请求选项，包括 signal 和 timeout。',
            },
          ],
        },
      ],
    },
    {
      name: 'listResourceTemplates',
      type: `async (options?: {
        options?: RequestOptions;
      }) => Promise<ListResourceTemplatesResult>`,
      description:
        '列出 MCP 服务器提供的所有资源模板。',
      properties: [
        {
          type: 'options',
          parameters: [
            {
              name: 'options',
              type: 'RequestOptions',
              isOptional: true,
              description:
                '可选请求选项，包括 signal 和 timeout。',
            },
          ],
        },
      ],
    },
    {
      name: 'experimental_listPrompts',
      type: `async (options?: {
        params?: PaginatedRequest['params'];
        options?: RequestOptions;
      }) => Promise<ListPromptsResult>`,
      description:
        '列出 MCP 服务器提供的提示。此方法是实验性的，未来可能会更改。',
      properties: [
        {
          type: 'options',
          parameters: [
            {
              name: 'params',
              type: "PaginatedRequest['params']",
              isOptional: true,
              description: '可选分页参数，包括 cursor。',
            },
            {
              name: 'options',
              type: 'RequestOptions',
              isOptional: true,
              description:
                '可选请求选项，包括 signal 和 timeout。',
            },
          ],
        },
      ],
    },
    {
      name: 'experimental_getPrompt',
      type: `async (args: {
        name: string;
        arguments?: Record<string, unknown>;
        options?: RequestOptions;
      }) => Promise<GetPromptResult>`,
      description:
        '按名称检索提示，并可选择传递 arguments。此方法是实验性的，未来可能会更改。',
      properties: [
        {
          type: 'args',
          parameters: [
            {
              name: 'name',
              type: 'string',
              description: '要检索的提示名称。',
            },
            {
              name: 'arguments',
              type: 'Record<string, unknown>',
              isOptional: true,
              description: '用于填充提示的可选 arguments。',
            },
            {
              name: 'options',
              type: 'RequestOptions',
              isOptional: true,
              description:
                '可选请求选项，包括 signal 和 timeout。',
            },
          ],
        },
      ],
    },
    {
      name: 'onElicitationRequest',
      type: `(
        schema: typeof ElicitationRequestSchema,
        handler: (request: ElicitationRequest) => Promise<ElicitResult> | ElicitResult
      ) => void`,
      description:
        '为来自 MCP 服务器的 elicitation 请求注册处理程序。当服务器在工具执行期间需要额外输入时，该处理程序会接收请求。',
      properties: [
        {
          type: 'parameters',
          parameters: [
            {
              name: 'schema',
              type: 'typeof ElicitationRequestSchema',
              description:
                '用于验证请求的 schema。必须是 ElicitationRequestSchema。',
            },
            {
              name: 'handler',
              type: '(request: ElicitationRequest) => Promise<ElicitResult> | ElicitResult',
              description:
                '处理 elicitation 请求的函数。请求包含 message 和 requestedSchema。处理程序必须返回一个包含 action（"accept"、"decline" 或 "cancel"）的对象，并在接受时可选返回 content。',
            },
          ],
        },
      ],
    },
    {
      name: 'close',
      type: '() => Promise<void>',
      description:
        '关闭与 MCP 服务器的连接并清理资源。',
    },
  ]}
/>

## 示例

```typescript
import { createMCPClient } from '@ai-sdk/mcp';
import { generateText } from 'ai';
import { Experimental_StdioMCPTransport } from '@ai-sdk/mcp/mcp-stdio';

let client;

try {
  client = await createMCPClient({
    transport: new Experimental_StdioMCPTransport({
      command: 'node server.js',
    }),
  });

  const tools = await client.tools();

  const response = await generateText({
    model: __MODEL__,
    tools,
    messages: [{ role: 'user', content: 'Query the data' }],
  });

  console.log(response);
} catch (error) {
  console.error('Error:', error);
} finally {
  // ensure the client is closed even if an error occurs
  if (client) {
    await client.close();
  }
}
```

## 错误处理

客户端会在以下情况下抛出 `MCPClientError`：

- 客户端初始化失败
- 协议版本不匹配
- 缺少服务器能力
- 连接失败

对于工具执行，错误会作为 `CallToolError` 错误传播。

对于未知错误，客户端会暴露一个 `onUncaughtError` 回调，可用于手动记录或处理未被已知错误类型覆盖的错误。
