---
title: createProviderRegistry
description: 用于管理多个 provider 和模型的注册表（API 参考）
---

# `createProviderRegistry()`

使用多个 provider 和模型时，通常需要
在一个中心位置管理它们，并通过简单的字符串 id 访问模型。

`createProviderRegistry` 允许你创建一个包含多个 provider 的注册表，
并可通过 `providerId:modelId` 格式的 id 访问它们。

在 TypeScript 中，注册表模型 ID 会从已注册的 provider ID 推断出来。
当 provider 暴露字面量模型 ID 类型时，编辑器可以建议组合后的
`providerId:modelId` 值。

### 设置

你可以使用 `createProviderRegistry` 创建包含多个 provider 和模型的注册表。

```ts
import { anthropic } from '@ai-sdk/anthropic';
import { createOpenAI } from '@ai-sdk/openai';
import { createProviderRegistry } from 'ai';

export const registry = createProviderRegistry({
  // register provider with prefix and default setup:
  anthropic,

  // register provider with prefix and custom setup:
  openai: createOpenAI({
    apiKey: process.env.OPENAI_API_KEY,
  }),
});
```

### 自定义分隔符

默认情况下，注册表使用 `:` 作为 provider 和模型 ID 之间的分隔符。你可以通过传入 `separator` 选项自定义该分隔符：

```ts
const registry = createProviderRegistry(
  {
    anthropic,
    openai,
  },
  { separator: ' > ' },
);

// Now you can use the custom separator
const model = registry.languageModel('anthropic > claude-3-opus-20240229');
```

### 语言模型

你可以使用注册表上的 `languageModel` 方法访问语言模型。
provider id 会成为模型 id 的前缀：`providerId:modelId`。

```ts highlight={"5"}
import { generateText } from 'ai';
import { registry } from './registry';

const { text } = await generateText({
  model: registry.languageModel('openai:gpt-4.1'),
  prompt: 'Invent a new holiday and describe its traditions.',
});
```

### 文本嵌入模型

你可以使用注册表上的 `.embeddingModel` 方法访问文本嵌入模型。
provider id 会成为模型 id 的前缀：`providerId:modelId`。

```ts highlight={"5"}
import { embed } from 'ai';
import { registry } from './registry';

const { embedding } = await embed({
  model: registry.embeddingModel('openai:text-embedding-3-small'),
  value: 'sunny day at the beach',
});
```

### 图像模型

你可以使用注册表上的 `imageModel` 方法访问图像模型。
provider id 会成为模型 id 的前缀：`providerId:modelId`。

```ts highlight={"5"}
import { generateImage } from 'ai';
import { registry } from './registry';

const { image } = await generateImage({
  model: registry.imageModel('openai:dall-e-3'),
  prompt: 'A beautiful sunset over a calm ocean',
});
```

### 视频模型

你可以使用注册表上的 `videoModel` 方法访问视频模型。
provider id 会成为模型 id 的前缀：`providerId:modelId`。

```ts highlight={"5"}
import { fal } from '@ai-sdk/fal';
import { createProviderRegistry, experimental_generateVideo } from 'ai';

const registry = createProviderRegistry({ fal });

const { videos } = await experimental_generateVideo({
  model: registry.videoModel('fal:luma-dream-machine/ray-2'),
  prompt: 'A cat walking on a beach at sunset',
});
```

### 文件和 skills

你可以通过调用以下方法访问 provider 的 files 和 skills 接口
`registry.files(providerId)` and `registry.skills(providerId)`.

## 导入

<Snippet text={`import { createProviderRegistry } from "ai"`} prompt={false} />

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'providers',
      type: 'Record<string, Provider>',
      description:
        'provider 的唯一标识符。它在注册表中应是唯一的。',
      properties: [
        {
          type: 'Provider',
          parameters: [
            {
              name: 'languageModel',
              type: '(id: string) => LanguageModel',
              description:
                '根据 id 返回语言模型的函数。',
            },
            {
              name: 'embeddingModel',
              type: '(id: string) => EmbeddingModel<string>',
              description:
                '根据 id 返回文本嵌入模型的函数。',
            },
            {
              name: 'imageModel',
              type: '(id: string) => ImageModel',
              description: '根据 id 返回图像模型的函数。',
            },
            {
              name: 'transcriptionModel',
              type: '(id: string) => TranscriptionModel',
              isOptional: true,
              description:
                '根据 id 返回转录模型的函数。',
            },
            {
              name: 'speechModel',
              type: '(id: string) => SpeechModel',
              isOptional: true,
              description: '根据 id 返回语音模型的函数。',
            },
            {
              name: 'rerankingModel',
              type: '(id: string) => RerankingModel',
              isOptional: true,
              description:
                '根据 id 返回重排序模型的函数。',
            },
            {
              name: 'videoModel',
              type: '(id: string) => VideoModelV4',
              isOptional: true,
              description: '根据 id 返回视频模型的函数。',
            },
            {
              name: 'files',
              type: '() => FilesV4',
              isOptional: true,
              description:
                '返回 provider files API 接口的函数。',
            },
            {
              name: 'skills',
              type: '() => SkillsV4',
              isOptional: true,
              description:
                '返回 provider skills API 接口的函数。',
            },
          ],
        },
      ],
    },
    {
      name: 'options',
      type: 'object',
      isOptional: true,
      description: '注册表的可选配置。',
      properties: [
        {
          type: 'Options',
          parameters: [
            {
              name: 'separator',
              type: 'string',
              isOptional: true,
              description:
                'provider 与模型 ID 之间的自定义分隔符。默认值为 ":"。',
            },
            {
              name: 'languageModelMiddleware',
              type: 'LanguageModelMiddleware | LanguageModelMiddleware[]',
              isOptional: true,
              description:
                '用于包装从注册表获取的所有语言模型的 middleware。',
            },
            {
              name: 'imageModelMiddleware',
              type: 'ImageModelMiddleware | ImageModelMiddleware[]',
              isOptional: true,
              description:
                '用于包装从注册表获取的所有图像模型的 middleware。',
            },
          ],
        },
      ],
    },
  ]}
/>

### 返回值

`createProviderRegistry` 函数返回一个 `Provider` 实例。它包含以下方法：

<PropertiesTable
  content={[
    {
      name: 'languageModel',
      type: '(id: string) => LanguageModel',
      description:
        '根据 id 返回语言模型的函数（格式：providerId:modelId）',
    },
    {
      name: 'embeddingModel',
      type: '(id: string) => EmbeddingModel<string>',
      description:
        '根据 id 返回文本嵌入模型的函数（格式：providerId:modelId）',
    },
    {
      name: 'imageModel',
      type: '(id: string) => ImageModel',
      description:
        '根据 id 返回图像模型的函数（格式：providerId:modelId）',
    },
    {
      name: 'transcriptionModel',
      type: '(id: string) => TranscriptionModel',
      description:
        '根据 id 返回转录模型的函数（格式：providerId:modelId）',
    },
    {
      name: 'speechModel',
      type: '(id: string) => SpeechModel',
      description:
        '根据 id 返回语音模型的函数（格式：providerId:modelId）',
    },
    {
      name: 'rerankingModel',
      type: '(id: string) => RerankingModel',
      description:
        '根据 id 返回重排序模型的函数（格式：providerId:modelId）',
    },
    {
      name: 'videoModel',
      type: '(id: string) => VideoModelV4',
      description:
        '根据 id 返回视频模型的函数（格式：providerId:modelId）',
    },
    {
      name: 'files',
      type: '(providerId: string) => FilesV4',
      description:
        '根据 provider id 返回 provider files API 接口的函数。',
    },
    {
      name: 'skills',
      type: '(providerId: string) => SkillsV4',
      description:
        '根据 provider id 返回 provider skills API 接口的函数。',
    },
  ]}
/>
