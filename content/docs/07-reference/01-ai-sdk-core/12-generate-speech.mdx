---
title: generateSpeech
description: generateSpeech 的 API 参考。
---

# `generateSpeech()`

<Note type="warning">`generateSpeech` 是实验性功能。</Note>

从文本生成语音音频。

```ts
import { experimental_generateSpeech as generateSpeech } from 'ai';
import { openai } from '@ai-sdk/openai';

const { audio } = await generateSpeech({
  model: openai.speech('tts-1'),
  text: 'Hello from the AI SDK!',
  voice: 'alloy',
});

console.log(audio);
```

## 示例

### OpenAI

```ts
import { experimental_generateSpeech as generateSpeech } from 'ai';
import { openai } from '@ai-sdk/openai';

const { audio } = await generateSpeech({
  model: openai.speech('tts-1'),
  text: 'Hello from the AI SDK!',
  voice: 'alloy',
});
```

### ElevenLabs

```ts
import { experimental_generateSpeech as generateSpeech } from 'ai';
import { elevenLabs } from '@ai-sdk/elevenlabs';

const { audio } = await generateSpeech({
  model: elevenLabs.speech('eleven_multilingual_v2'),
  text: 'Hello from the AI SDK!',
  voice: 'your-voice-id', // Required: get this from your ElevenLabs account
});
```

## 导入

<Snippet
  text={`import { experimental_generateSpeech as generateSpeech } from "ai"`}
  prompt={false}
/>

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'model',
      type: 'SpeechModelV4',
      description: '要使用的语音模型。',
    },
    {
      name: 'text',
      type: 'string',
      description: '用于生成语音的文本。',
    },
    {
      name: 'voice',
      type: 'string',
      isOptional: true,
      description: '语音使用的声音。',
    },
    {
      name: 'outputFormat',
      type: 'string',
      isOptional: true,
      description:
        '语音使用的输出格式，例如 "mp3"、"wav" 等。',
    },
    {
      name: 'instructions',
      type: 'string',
      isOptional: true,
      description: '语音生成的指令。',
    },
    {
      name: 'speed',
      type: 'number',
      isOptional: true,
      description: '语音生成的速度。',
    },
    {
      name: 'language',
      type: 'string',
      isOptional: true,
      description:
        '语音生成的语言。应为 ISO 639-1 语言代码（例如 "en"、"es"、"fr"），或使用 "auto" 自动检测语言。provider 支持情况各不相同。',
    },
    {
      name: 'providerOptions',
      type: 'Record<string, JSONObject>',
      isOptional: true,
      description: '额外的 provider 专属选项。',
    },
    {
      name: 'maxRetries',
      type: 'number',
      isOptional: true,
      description: '最大重试次数。默认值：2。',
    },
    {
      name: 'abortSignal',
      type: 'AbortSignal',
      isOptional: true,
      description: '可选的中止信号，可用于取消调用。',
    },
    {
      name: 'headers',
      type: 'Record<string, string>',
      isOptional: true,
      description: '请求的额外 HTTP 标头。',
    },
  ]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'audio',
      type: 'GeneratedAudioFile',
      description: '生成的音频。',
      properties: [
        {
          type: 'GeneratedAudioFile',
          parameters: [
            {
              name: 'base64',
              type: 'string',
              description: '以 base64 编码字符串表示的音频。',
            },
            {
              name: 'uint8Array',
              type: 'Uint8Array',
              description: '以 Uint8Array 表示的音频。',
            },
            {
              name: 'mediaType',
              type: 'string',
              description: '音频的媒体类型（例如 "audio/mpeg"）。',
            },
            {
              name: 'format',
              type: 'string',
              description: '音频格式（例如 "mp3"）。',
            },
          ],
        },
      ],
    },
    {
      name: 'warnings',
      type: 'Warning[]',
      description:
        '模型 provider 返回的警告（例如不支持的设置）。',
    },
    {
      name: 'providerMetadata',
      type: 'Record<string, JSONObject>',
      isOptional: true,
      description:
        '来自 provider 的可选元数据。外层键是 provider 名称，内层值是元数据。具体细节取决于 provider。',
    },
    {
      name: 'responses',
      type: 'Array<SpeechModelResponseMetadata>',
      description:
        '来自 provider 的响应元数据。如果对模型发起了多次调用，可能会有多个响应。',
      properties: [
        {
          type: 'SpeechModelResponseMetadata',
          parameters: [
            {
              name: 'timestamp',
              type: 'Date',
              description: '生成响应开始时的时间戳。',
            },
            {
              name: 'modelId',
              type: 'string',
              description:
                '用于生成响应的响应模型 ID。',
            },
            {
              name: 'body',
              isOptional: true,
              type: 'unknown',
              description: '可选响应体。',
            },
            {
              name: 'headers',
              type: 'Record<string, string>',
              isOptional: true,
              description: '响应标头。',
            },
          ],
        },
      ],
    },
  ]}
/>
