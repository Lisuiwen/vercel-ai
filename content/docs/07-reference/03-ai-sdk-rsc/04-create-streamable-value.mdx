---
title: createStreamableValue
description: AI SDK RSC 中 createStreamableValue 函数的参考
---

# `createStreamableValue`

<Note type="warning">
  AI SDK RSC 目前处于实验阶段。我们建议在生产环境中使用 [AI SDK
  UI](/docs/ai-sdk-ui/overview)。关于从 RSC 迁移到 UI 的指导，请参阅我们的[迁移指南](/docs/ai-sdk-rsc/migrating-to-ui)。
</Note>

创建一个从服务器向客户端发送值的流。该值可以是任何可序列化数据。

## 导入

<Snippet
  text={`import { createStreamableValue } from "@ai-sdk/rsc"`}
  prompt={false}
/>

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'value',
      type: 'any',
      description: 'RSC 支持的任何数据。例如 JSON。',
    },
  ]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'value',
      type: 'StreamableValue',
      description:
        'streamable 的值。它可以从 Server Action 返回并由客户端接收。要读取流式值，请使用 `readStreamableValue` 或 `useStreamableValue` API。',
    },
  ]}
/>

### 方法

<PropertiesTable
  content={[
    {
      name: 'update',
      type: '(value: T) => StreamableValueWrapper',
      description: '用新值更新当前值。',
    },
    {
      name: 'append',
      type: '(value: T) => StreamableValueWrapper',
      description:
        '向当前值追加一个增量字符串。这要求 streamable 的当前值是字符串。',
    },
    {
      name: 'done',
      type: '(value?: T) => StreamableValueWrapper',
      description:
        '将值标记为完成。你可以不带任何参数调用它，也可以传入一个新值作为最终状态。一旦调用，该值就不能再更新或追加。必须始终调用此方法，否则响应会停留在加载状态。',
    },
    {
      name: 'error',
      type: '(error: any) => StreamableValueWrapper',
      description:
        '表示值流中出现错误。当通过 `readStreamableValue` 或 `useStreamableValue` 消费时，它会在客户端抛出。',
    },
  ]}
/>
