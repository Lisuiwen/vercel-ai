---
title: AI_NoObjectGeneratedError
description: 了解如何修复 AI_NoObjectGeneratedError
---

# AI_NoObjectGeneratedError

此错误在 AI 提供方未能生成可解析且符合 schema 的对象时发生。
可能由以下原因引起：

- 模型未能生成响应。
- 模型生成的响应无法解析。
- 模型生成的响应无法通过 schema 校验。

## 属性

- `message`：错误消息（可选，默认为 `'No object generated.'`）。
- `text`：模型生成的文本。根据对象生成模式，可能是原始文本或工具调用文本（可选）。
- `response`：关于语言模型响应的元数据，包括响应 id、时间戳和模型（构造函数中必填）。
- `usage`：请求的 token 用量（构造函数中必填）。
- `finishReason`：请求的结束原因。例如，若模型生成了最大 token 数，则为 `'length'`，这可能导致 JSON 解析错误（构造函数中必填）。
- `cause`：错误原因（例如 JSON 解析错误）。可用于更详细的错误处理（可选）。

## 如何检查此错误

你可以通过以下方式检查错误是否为 `AI_NoObjectGeneratedError` 的实例：

```typescript
import { generateText, NoObjectGeneratedError, Output } from 'ai';

try {
  await generateText({ model, output: Output.object({ schema }), prompt });
} catch (error) {
  if (NoObjectGeneratedError.isInstance(error)) {
    console.log('NoObjectGeneratedError');
    console.log('Cause:', error.cause);
    console.log('Text:', error.text);
    console.log('Response:', error.response);
    console.log('Usage:', error.usage);
    console.log('Finish Reason:', error.finishReason);
  }
}
```
