---
title: createUIMessageStream
description: createUIMessageStream 的 API 参考。
---

# `createUIMessageStream`

`createUIMessageStream` 函数允许你创建用于 UI message 的可读流，并支持消息合并、错误处理和完成回调等高级功能。

## 导入

<Snippet text={`import { createUIMessageStream } from "ai"`} prompt={false} />

## 示例

```tsx
const existingMessages: UIMessage[] = [
  /* ... */
];

const stream = createUIMessageStream({
  async execute({ writer }) {
    // Start a text message
    // Note: The id must be consistent across text-start, text-delta, and text-end steps
    // This allows the system to correctly identify they belong to the same text block
    writer.write({
      type: 'text-start',
      id: 'example-text',
    });

    // Write a message chunk
    writer.write({
      type: 'text-delta',
      id: 'example-text',
      delta: 'Hello',
    });

    // End the text message
    writer.write({
      type: 'text-end',
      id: 'example-text',
    });

    // Merge another stream from streamText
    const result = streamText({
      model: __MODEL__,
      prompt: 'Write a haiku about AI',
    });

    writer.merge(result.toUIMessageStream());
  },
  onError: error => `Custom error: ${error.message}`,
  originalMessages: existingMessages,
  onFinish: ({ messages, isContinuation, responseMessage }) => {
    console.log('Stream finished with messages:', messages);
  },
});
```

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'execute',
      type: '(options: { writer: UIMessageStreamWriter }) => Promise<void> | void',
      description:
        '接收 writer 实例的函数，可使用该实例将 UI message chunk 写入流。',
      properties: [
        {
          type: 'UIMessageStreamWriter',
          parameters: [
            {
              name: 'write',
              type: '(part: UIMessageChunk) => void',
              description: '将 UI message chunk 写入流。',
            },
            {
              name: 'merge',
              type: '(stream: ReadableStream<UIMessageChunk>) => void',
              description:
                '将另一个 UI message stream 的内容合并到此流中。',
            },
            {
              name: 'onError',
              type: '(error: unknown) => string',
              description:
                '流 writer 用于处理合并流中错误的错误处理器。',
            },
          ],
        },
      ],
    },
    {
      name: 'onError',
      type: '(error: unknown) => string',
      description:
        '处理错误并返回错误消息字符串的函数。默认返回错误消息。',
    },
    {
      name: 'originalMessages',
      type: 'UIMessage[] | undefined',
      description:
        '原始消息。提供后会假定使用持久化模式，并为响应消息提供消息 ID。',
    },
    {
      name: 'onFinish',
      type: '(options: { messages: UIMessage[]; isContinuation: boolean; isAborted: boolean; responseMessage: UIMessage; finishReason?: FinishReason }) => PromiseLike<void> | void',
      description:
        '流完成时调用的回调函数。',
      properties: [
        {
          type: 'FinishOptions',
          parameters: [
            {
              name: 'messages',
              type: 'UIMessage[]',
              description: '更新后的 UI message 列表。',
            },
            {
              name: 'isContinuation',
              type: 'boolean',
              description:
                '表示响应消息是最后一条原始消息的延续，还是创建了新消息。',
            },
            {
              name: 'isAborted',
              type: 'boolean',
              description: '表示流是否已中止。',
            },
            {
              name: 'responseMessage',
              type: 'UIMessage',
              description:
                '作为响应发送给客户端的消息（如果扩展了原始消息，则包含原始消息）。',
            },
            {
              name: 'finishReason',
              type: 'FinishReason | undefined',
              description:
                "生成完成的原因。取值之一：'stop'、'length'、'content-filter'、'tool-calls'、'error' 或 'other'。",
            },
          ],
        },
      ],
    },
    {
      name: 'generateId',
      type: 'IdGenerator | undefined',
      description:
        '用于为消息生成唯一 ID 的函数。未提供时使用默认 ID 生成器。',
    },
  ]}
/>

### 返回值

`ReadableStream<UIMessageChunk>`

发出 UI message chunk 的可读流。该流会自动处理错误传播、多个流的合并，并在所有操作完成后正确清理。
