---
title: experimental_MCPAppRenderer
description: 使用 @ai-sdk/react 渲染 MCP Apps 的 API 参考。
---

# `experimental_MCPAppRenderer`

<Note type="warning">
  `experimental_MCPAppRenderer` 是实验性功能，可能会在未来
  版本中发生变化。
</Note>

`experimental_MCPAppRenderer` 为 AI SDK 工具 UI part 渲染 MCP App。它会检测工具 part 上的 MCP App 元数据，加载 app 资源，在沙箱代理 iframe 中渲染 app，并在 iframe 与宿主应用之间桥接 MCP Apps JSON-RPC 消息。

对于没有 MCP App 元数据的工具 part，该组件会渲染 `fallback`。

## 导入

<Snippet
  text={`import { experimental_MCPAppRenderer as MCPAppRenderer } from "@ai-sdk/react"`}
  prompt={false}
/>

## Example

```tsx
'use client';

import {
  experimental_MCPAppRenderer as MCPAppRenderer,
  type MCPAppBridgeHandlers,
  type MCPAppMetadata,
  type MCPAppResource,
  type MCPAppSandboxConfig,
} from '@ai-sdk/react';
import { isToolUIPart } from 'ai';

const sandbox = {
  url: '/mcp-app-sandbox',
  className: 'h-80 w-full rounded-lg border',
  style: { border: 0 },
} satisfies MCPAppSandboxConfig;

async function loadResource(app: MCPAppMetadata): Promise<MCPAppResource> {
  const response = await fetch('/api/mcp-app-host/read-resource', {
    method: 'POST',
    body: JSON.stringify({ uri: app.resourceUri }),
  });

  if (!response.ok) {
    throw new Error('Failed to load MCP App resource');
  }

  return response.json();
}

const handlers: MCPAppBridgeHandlers = {
  callTool: params =>
    fetch('/api/mcp-app-host/call-tool', {
      method: 'POST',
      body: JSON.stringify(params),
    }).then(response => response.json()),
  openLink: ({ url }) => {
    window.open(url, '_blank', 'noopener,noreferrer');
    return {};
  },
};

export function MessagePart({ part }: { part: unknown }) {
  if (!isToolUIPart(part)) {
    return null;
  }

  return (
    <MCPAppRenderer
      part={part}
      loadResource={loadResource}
      handlers={handlers}
      sandbox={sandbox}
      fallback={null}
    />
  );
}
```

## Props

<PropertiesTable
  content={[
    {
      name: 'part',
      type: 'ToolUIPart<UITools> | DynamicToolUIPart',
      description:
        'The AI SDK tool UI part. The renderer looks for MCP App metadata in `part.toolMetadata.mcp.app`.',
    },
    {
      name: 'sandbox',
      type: 'MCPAppSandboxConfig',
      description:
        '用于托管 app 的外层沙箱代理 iframe 配置。',
    },
    {
      name: 'resource',
      type: 'MCPAppResource',
      isOptional: true,
      description:
        '预加载的 MCP App 资源。请提供 `resource` 或 `loadResource` 之一。',
    },
    {
      name: 'loadResource',
      type: '(app: MCPAppMetadata) => Promise<MCPAppResource>',
      isOptional: true,
      description:
        '为工具 part 上找到的 app 元数据加载 MCP App 资源。',
    },
    {
      name: 'handlers',
      type: 'MCPAppBridgeHandlers',
      isOptional: true,
      description:
        '用于处理 iframe 请求的回调，例如 `tools/call`、`resources/read`、`ui/open-link` 和显示模式变更。',
    },
    {
      name: 'hostInfo',
      type: '{ name: string; version: string }',
      isOptional: true,
      description:
        '在 `ui/initialize` 握手期间返回给 app 的宿主身份。',
    },
    {
      name: 'hostContext',
      type: 'MCPAppHostContext',
      isOptional: true,
      description:
        '发送给 app 的宿主上下文，例如主题、显示模式和可用显示模式。',
    },
    {
      name: 'fallback',
      type: 'ReactNode',
      isOptional: true,
      description:
        '在资源加载中、加载失败，或工具 part 不是 MCP App 时渲染。',
    },
  ]}
/>

## 沙箱配置

<PropertiesTable
  content={[
    {
      name: 'url',
      type: 'string | URL',
      description:
        '沙箱代理 iframe 的 URL。代理从宿主接收 app HTML，并创建内部 app iframe。',
    },
    {
      name: 'title',
      type: 'string',
      isOptional: true,
      description: '可访问的 iframe 标题。',
    },
    {
      name: 'className',
      type: 'string',
      isOptional: true,
      description: '应用于外层 iframe 的 class 名称。',
    },
    {
      name: 'style',
      type: 'CSSProperties',
      isOptional: true,
      description: '应用于外层 iframe 的内联样式。',
    },
    {
      name: 'targetOrigin',
      type: 'string',
      isOptional: true,
      description:
        '用于 `postMessage` 的目标 origin。默认值为 `*`；生产环境中请将其设置为你的沙箱 origin。',
    },
    {
      name: 'outerSandbox',
      type: 'string',
      isOptional: true,
      description:
        '外层代理 iframe 的 sandbox 属性。默认值为 `allow-scripts allow-same-origin allow-forms`。',
    },
    {
      name: 'innerSandbox',
      type: 'string',
      isOptional: true,
      description:
        '发送给代理并用于内部 app iframe 的 sandbox 属性。默认值为 `allow-scripts allow-forms`。',
    },
  ]}
/>

## Bridge 处理器

`experimental_MCPAppRenderer` 使用这些处理器响应 iframe 请求。生产环境中，由服务端支持的处理器应在调用 MCP server 前验证授权和 MCP Apps 工具可见性。

<PropertiesTable
  content={[
    {
      name: 'allowedTools',
      type: 'string[]',
      isOptional: true,
      description:
        '转发 `tools/call` 请求前检查的可选客户端 allowlist。',
    },
    {
      name: 'callTool',
      type: '(params: MCPAppToolCallParams) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理 app 发起的 `tools/call` 请求。',
    },
    {
      name: 'readResource',
      type: '(params: { uri: string }) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理 app 发起的 `resources/read` 请求。',
    },
    {
      name: 'listResources',
      type: '(params?: unknown) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理 app 发起的 `resources/list` 请求。',
    },
    {
      name: 'openLink',
      type: '(params: { url: string }) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理 app 发起的 `ui/open-link` 请求。',
    },
    {
      name: 'sendMessage',
      type: '(params: unknown) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理 app 发起的 `ui/message` 请求。',
    },
    {
      name: 'updateModelContext',
      type: '(params: unknown) => Promise<unknown> | unknown',
      isOptional: true,
      description: '处理 app 发起的 `ui/update-model-context` 请求。',
    },
    {
      name: 'requestDisplayMode',
      type: "(params: { mode: 'inline' | 'fullscreen' | 'pip' }) => Promise<{ mode: MCPAppDisplayMode }> | { mode: MCPAppDisplayMode }",
      isOptional: true,
      description: '处理 app 发起的 `ui/request-display-mode` 请求。',
    },
    {
      name: 'onSizeChange',
      type: '(params: { width?: number; height?: number }) => void',
      isOptional: true,
      description: '当 app 发送尺寸变更通知时调用。',
    },
    {
      name: 'onInitialized',
      type: '() => void',
      isOptional: true,
      description: '当 app 发送 `ui/notifications/initialized` 后调用。',
    },
    {
      name: 'onRequestTeardown',
      type: '(params: unknown) => void',
      isOptional: true,
      description: '当 app 请求 teardown 时调用。',
    },
    {
      name: 'onLog',
      type: '(params: unknown) => void',
      isOptional: true,
      description: '当 app 发送日志通知时调用。',
    },
    {
      name: 'onError',
      type: '(error: Error) => void',
      isOptional: true,
      description:
        '处理受支持 iframe 请求失败时调用。',
    },
  ]}
/>

## 另请参阅

<ExampleLinks
  examples={[
    {
      title: 'MCP Apps 指南',
      link: '/docs/ai-sdk-core/mcp-apps',
    },
    {
      title: 'MCP Apps 辅助函数',
      link: '/docs/reference/ai-sdk-core/mcp-apps',
    },
  ]}
/>
