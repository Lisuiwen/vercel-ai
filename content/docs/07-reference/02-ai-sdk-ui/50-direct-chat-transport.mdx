---
title: DirectChatTransport
description: DirectChatTransport 类的 API 参考。
---

# `DirectChatTransport`

一种直接在进程内与 [Agent](/docs/reference/ai-sdk-core/agent) 通信、无需经过 HTTP 的 transport。这适用于：

- 服务端渲染场景
- 无网络测试
- 单进程应用

与向 API 端点发送 HTTP 请求的 `DefaultChatTransport` 不同，`DirectChatTransport` 会直接调用 agent 的 `stream()` 方法，并将结果转换为 UI message stream。

```tsx
import { useChat } from '@ai-sdk/react';
import { DirectChatTransport, ToolLoopAgent } from 'ai';
__PROVIDER_IMPORT__;

const agent = new ToolLoopAgent({
  model: __MODEL__,
  instructions: 'You are a helpful assistant.',
});

export default function Chat() {
  const { messages, sendMessage, status } = useChat({
    transport: new DirectChatTransport({ agent }),
  });

  // ... render chat UI
}
```

## 导入

<Snippet text={`import { DirectChatTransport } from "ai"`} prompt={false} />

## 构造函数

### 参数

<PropertiesTable
  content={[
    {
      name: 'agent',
      type: 'Agent',
      isRequired: true,
      description:
        '用于生成响应的 Agent 实例。每条消息都会通过 `stream()` 调用该 agent。',
    },
    {
      name: 'options',
      type: 'CALL_OPTIONS',
      isOptional: true,
      description:
        '调用 agent 时传递给它的选项。这些是在创建 agent 时定义的 agent 专属选项。',
    },
    {
      name: 'originalMessages',
      type: 'UIMessage[]',
      isOptional: true,
      description:
        '原始消息。提供后会假定使用持久化模式，并为响应消息提供消息 ID。',
    },
    {
      name: 'generateMessageId',
      type: 'IdGenerator',
      isOptional: true,
      description:
        '为响应消息生成消息 ID。未提供时，不会为响应消息设置消息 ID。',
    },
    {
      name: 'messageMetadata',
      type: '(options: { part: TextStreamPart }) => METADATA | undefined',
      isOptional: true,
      description:
        '提取将发送给客户端的消息元数据。在 `start` 和 `finish` 事件上调用。',
    },
    {
      name: 'sendReasoning',
      type: 'boolean',
      isOptional: true,
      description: '向客户端发送 reasoning part。默认值：true。',
    },
    {
      name: 'sendSources',
      type: 'boolean',
      isOptional: true,
      description: '向客户端发送 source part。默认值：false。',
    },
    {
      name: 'sendFinish',
      type: 'boolean',
      isOptional: true,
      description:
        '向客户端发送 finish 事件。如果你使用额外的 streamText 调用发送额外数据，请设置为 false。默认值：true。',
    },
    {
      name: 'sendStart',
      type: 'boolean',
      isOptional: true,
      description:
        '向客户端发送消息开始事件。如果你使用额外的 streamText 调用且消息开始事件已经发送，请设置为 false。默认值：true。',
    },
    {
      name: 'onError',
      type: '(error: unknown) => string',
      isOptional: true,
      description:
        "处理错误，例如记录日志。默认值：`() => 'An error occurred.'`。返回要包含在数据流中的错误消息。",
    },
  ]}
/>

## 方法

### `sendMessages()`

向 agent 发送消息并返回流式响应。此方法会验证 UI message 并将其转换为模型消息，调用 agent 的 `stream()` 方法，并将结果作为 UI message stream 返回。

```ts
const stream = await transport.sendMessages({
  chatId: 'chat-123',
  trigger: 'submit-message',
  messages: [...],
  abortSignal: controller.signal,
});
```

<PropertiesTable
  content={[
    {
      name: 'chatId',
      type: 'string',
      description: '聊天会话的唯一标识符。',
    },
    {
      name: 'trigger',
      type: "'submit-message' | 'regenerate-message'",
      description:
        '消息提交类型：新消息或重新生成。',
    },
    {
      name: 'messageId',
      type: 'string | undefined',
      description:
        '要重新生成的消息 ID；新消息则为 undefined。',
    },
    {
      name: 'messages',
      type: 'UIMessage[]',
      description:
        '表示对话历史的 UI message 数组。',
    },
    {
      name: 'abortSignal',
      type: 'AbortSignal | undefined',
      description: '需要时用于中止请求的信号。',
    },
    {
      name: 'headers',
      type: 'Record<string, string> | Headers',
      isOptional: true,
      description: '额外标头（DirectChatTransport 会忽略）。',
    },
    {
      name: 'body',
      type: 'object',
      isOptional: true,
      description:
        '额外 body 属性（DirectChatTransport 会忽略）。',
    },
    {
      name: 'metadata',
      type: 'unknown',
      isOptional: true,
      description: '自定义元数据（DirectChatTransport 会忽略）。',
    },
  ]}
/>

#### 返回值

返回 `Promise<ReadableStream<UIMessageChunk>>`，即可由 chat UI 处理的 UI message chunk 流。

### `reconnectToStream()`

Direct transport 不支持重连，因为没有可重新连接的持久服务端流。

#### 返回值

始终返回 `Promise<null>`。

## 示例

### 基本用法

```tsx
import { useChat } from '@ai-sdk/react';
import { DirectChatTransport, ToolLoopAgent } from 'ai';
import { openai } from '@ai-sdk/openai';

const agent = new ToolLoopAgent({
  model: openai('gpt-4o'),
  instructions: 'You are a helpful assistant.',
});

export default function Chat() {
  const { messages, sendMessage, status } = useChat({
    transport: new DirectChatTransport({ agent }),
  });

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role === 'user' ? 'User: ' : 'AI: '}
          {message.parts.map((part, index) =>
            part.type === 'text' ? <span key={index}>{part.text}</span> : null,
          )}
        </div>
      ))}
      <button onClick={() => sendMessage({ text: 'Hello!' })}>Send</button>
    </div>
  );
}
```

### 使用 Agent 工具

```tsx
import { useChat } from '@ai-sdk/react';
import { DirectChatTransport, ToolLoopAgent, tool } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

const weatherTool = tool({
  description: 'Get the current weather',
  parameters: z.object({
    location: z.string().describe('The city and state'),
  }),
  execute: async ({ location }) => {
    return `The weather in ${location} is sunny and 72°F.`;
  },
});

const agent = new ToolLoopAgent({
  model: openai('gpt-4o'),
  instructions: 'You are a helpful assistant with access to weather data.',
  tools: { weather: weatherTool },
});

export default function Chat() {
  const { messages, sendMessage } = useChat({
    transport: new DirectChatTransport({ agent }),
  });

  // ... render chat UI with tool results
}
```

### 使用自定义 Agent 选项

```tsx
import { useChat } from '@ai-sdk/react';
import { DirectChatTransport, ToolLoopAgent } from 'ai';
import { openai } from '@ai-sdk/openai';

const agent = new ToolLoopAgent<{ userId: string }>({
  model: openai('gpt-4o'),
  prepareCall: ({ options, ...rest }) => ({
    ...rest,
    providerOptions: {
      openai: { user: options.userId },
    },
  }),
});

export default function Chat({ userId }: { userId: string }) {
  const { messages, sendMessage } = useChat({
    transport: new DirectChatTransport({
      agent,
      options: { userId },
    }),
  });

  // ... render chat UI
}
```

### 使用 Reasoning

```tsx
import { useChat } from '@ai-sdk/react';
import { DirectChatTransport, ToolLoopAgent } from 'ai';
import { openai } from '@ai-sdk/openai';

const agent = new ToolLoopAgent({
  model: openai('o1-preview'),
});

export default function Chat() {
  const { messages, sendMessage } = useChat({
    transport: new DirectChatTransport({
      agent,
      sendReasoning: true,
    }),
  });

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.parts.map((part, index) => {
            if (part.type === 'text') {
              return <p key={index}>{part.text}</p>;
            }
            if (part.type === 'reasoning') {
              return (
                <pre key={index} style={{ opacity: 0.6 }}>
                  {part.text}
                </pre>
              );
            }
            return null;
          })}
        </div>
      ))}
    </div>
  );
}
```
