---
title: useCompletion
description: useCompletion hook 的 API 参考。
---

# `useCompletion()`

允许你为应用创建基于文本补全的能力。它支持从 AI provider 流式传输文本补全，管理聊天输入状态，并在收到新消息时自动更新 UI。

## 导入

<Tabs items={['React', 'Svelte', 'Vue', 'Angular']}>
  <Tab>
    <Snippet
      text="import { useCompletion } from '@ai-sdk/react'"
      dark
      prompt={false}
    />
  </Tab>
  <Tab>
    <Snippet
      text="import { Completion } from '@ai-sdk/svelte'"
      dark
      prompt={false}
    />
  </Tab>
  <Tab>
    <Snippet
      text="import { useCompletion } from '@ai-sdk/vue'"
      dark
      prompt={false}
    />
  </Tab>
  <Tab>
    <Snippet
      text="import { Completion } from '@ai-sdk/angular'"
      dark
      prompt={false}
    />
  </Tab>
</Tabs>

## API 签名

### 参数

<PropertiesTable
  content={[
    {
      name: 'api',
      type: "string = '/api/completion'",
      description:
        '用于生成文本的 API 端点。它可以是相对路径（以 `/` 开头）或绝对 URL。',
    },
    {
      name: 'id',
      type: 'string',
      description:
        'completion 的唯一标识符。如果未提供，将生成一个随机标识符。提供后，具有相同 `id` 的 `useCompletion` hook 将在组件之间共享状态。当多个组件显示同一个聊天流时，这很有用。',
    },
    {
      name: 'initialInput',
      type: 'string',
      description: '初始 prompt 输入的可选字符串。',
    },
    {
      name: 'initialCompletion',
      type: 'string',
      description: '初始 completion 结果的可选字符串。',
    },
    {
      name: 'onFinish',
      type: '(prompt: string, completion: string) => void',
      description:
        '当 completion 流结束时调用的可选回调函数。',
    },
    {
      name: 'onError',
      type: '(error: Error) => void',
      description:
        '当聊天流遇到错误时调用的可选回调。',
    },
    {
      name: 'headers',
      type: 'Record<string, string> | Headers',
      description:
        '要传递给 API 端点的可选 headers 对象。',
    },
    {
      name: 'body',
      type: 'object',
      description:
        '要传递给 API 端点的可选额外 body 对象。',
    },
    {
      name: 'credentials',
      type: "'omit' | 'same-origin' | 'include'",
      description:
        '设置请求所用 credentials 模式的可选字面量。默认值为 same-origin。',
    },
    {
      name: 'streamProtocol',
      type: "'text' | 'data'",
      isOptional: true,
      description:
        '设置要使用的流类型的可选字面量。默认值为 `data`。如果设置为 `text`，流将被视为文本流。请注意，无论使用哪种协议，`useCompletion` 都只通过 `completion` 字段暴露文本内容，服务器发出的非文本部分（例如 `data-*` parts）会被忽略。如果需要访问这些 parts，请使用 `useChat`。',
    },
    {
      name: 'fetch',
      type: 'FetchFunction',
      isOptional: true,
      description:
        '可选。用于 API 调用的自定义 fetch 函数。默认使用全局 fetch 函数。',
    },
    {
      name: 'experimental_throttle',
      type: 'number',
      isOptional: true,
      description:
        '仅 React。completion 和 data 更新的自定义节流等待时间，单位为毫秒。指定后，会限制流式传输期间 UI 更新的频率。默认值为 undefined，表示禁用节流。',
    },

]}
/>

### 返回值

<PropertiesTable
  content={[
    {
      name: 'completion',
      type: 'string',
      description: '当前文本 completion。',
    },
    {
      name: 'complete',
      type: '(prompt: string, options?: { headers?: Record<string, string> | Headers, body?: object }) => Promise<string | null | undefined>',
      description:
        '基于提供的 prompt 执行文本 completion 的函数。完成后返回 completion 结果。',
    },
    {
      name: 'error',
      type: 'undefined | Error',
      description: 'completion 过程中抛出的错误（如果有）。',
    },
    {
      name: 'setCompletion',
      type: '(completion: string) => void',
      description: '用于更新 `completion` 状态的函数。',
    },
    {
      name: 'stop',
      type: '() => void',
      description: '用于中止当前 API 请求的函数。',
    },
    {
      name: 'input',
      type: 'string',
      description: '输入字段的当前值。',
    },
    {
      name: 'setInput',
      type: 'React.Dispatch<React.SetStateAction<string>>',
      description: '用于更新输入值的函数。',
    },
    {
      name: 'handleInputChange',
      type: '(event: any) => void',
      description:
        "输入字段 `onChange` 事件的处理器，用于控制输入值。",
    },
    {
      name: 'handleSubmit',
      type: '(event?: { preventDefault?: () => void }) => void',
      description:
        '表单提交处理器，会自动重置输入字段并追加一条用户消息。',
    },
    {
      name: 'isLoading',
      type: 'boolean',
      description:
        '布尔标志，表示当前是否有 fetch 操作正在进行。',
    },
  ]}
/>
