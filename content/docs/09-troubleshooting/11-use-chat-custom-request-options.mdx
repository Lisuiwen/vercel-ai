---
title: useChat 无法应用自定义 headers、body、credentials
description: 排查 useChat 钩子中自定义请求配置不生效的问题
---

# useChat 无法应用自定义 headers、body、credentials

## 问题

使用 `useChat` 时，直接在钩子上配置的 headers、body、credentials 等自定义请求选项不会随请求发送：

```tsx
// These options are not sent with the request
const { messages, sendMessage } = useChat({
  headers: {
    Authorization: 'Bearer token123',
  },
  body: {
    user_id: '123',
  },
  credentials: 'include',
});
```

## 背景

`useChat` 配置请求选项的方式已变更。钩子本体上直接使用 `headers`、`body`、`credentials` 已不再支持。应改用带 `DefaultChatTransport` 的 `transport`，或在单次请求级别传入选项。

## 解决方案

正确配置 `useChat` 请求有三种方式：

### 方式 1：请求级配置（推荐用于动态值）

对会随时间变化的值，推荐在调用 `sendMessage` 时传入：

```tsx
const { messages, sendMessage } = useChat();

// Send options with each message
sendMessage(
  { text: input },
  {
    headers: {
      Authorization: `Bearer ${getAuthToken()}`, // Dynamic auth token
      'X-Request-ID': generateRequestId(),
    },
    body: {
      temperature: 0.7,
      max_tokens: 100,
      user_id: getCurrentUserId(), // Dynamic user ID
      sessionId: getCurrentSessionId(), // Dynamic session
    },
  },
);
```

这样每次请求都会带上最新值。

### 方式 2：钩子级静态配置

对组件生命周期内不变的值，使用 `DefaultChatTransport`：

```tsx
import { useChat } from '@ai-sdk/react';
import { DefaultChatTransport } from 'ai';

const { messages, sendMessage } = useChat({
  transport: new DefaultChatTransport({
    api: '/api/chat',
    headers: {
      'X-API-Version': 'v1', // Static API version
      'X-App-ID': 'my-app', // Static app identifier
    },
    body: {
      model: 'gpt-5.1', // Default model
      stream: true, // Static configuration
    },
    credentials: 'include', // Static credentials policy
  }),
});
```

### 方式 3：钩子级可解析函数

若需在钩子层使用动态值，可传入返回配置的函数。但一般而言，请求级配置更可靠：

```tsx
import { useChat } from '@ai-sdk/react';
import { DefaultChatTransport } from 'ai';

const { messages, sendMessage } = useChat({
  transport: new DefaultChatTransport({
    api: '/api/chat',
    headers: () => ({
      Authorization: `Bearer ${getAuthToken()}`,
      'X-User-ID': getCurrentUserId(),
    }),
    body: () => ({
      sessionId: getCurrentSessionId(),
      preferences: getUserPreferences(),
    }),
    credentials: () => (isAuthenticated() ? 'include' : 'same-origin'),
  }),
});
```

<Note>
  对会随组件状态变化的配置，推荐请求级配置（方式 1）。
  若使用钩子级函数，可考虑用 `useRef` 保存当前值，在配置函数中读取 `ref.current`。
</Note>

### 合并钩子级与请求级选项

请求级选项会覆盖钩子级选项：

```tsx
// Hook-level default configuration
const { messages, sendMessage } = useChat({
  transport: new DefaultChatTransport({
    api: '/api/chat',
    headers: {
      'X-API-Version': 'v1',
    },
    body: {
      model: 'gpt-5.1',
    },
  }),
});

// Override or add options per request
sendMessage(
  { text: input },
  {
    headers: {
      'X-API-Version': 'v2', // This overrides the hook-level header
      'X-Request-ID': '123', // This is added
    },
    body: {
      model: 'gpt-5-mini', // This overrides the hook-level body field
      temperature: 0.5, // This is added
    },
  },
);
```

更多说明见 [请求配置](/docs/ai-sdk-ui/chatbot#request-configuration) 文档。
