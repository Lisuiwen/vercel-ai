---
title: 工具调用与结构化输出
description: 排查在结合结构化输出生成时使用工具调用的问题
---

# 工具调用与结构化输出

## 问题

你可能希望同时使用工具调用与结构化输出生成。

## 背景

要在使用结构化输出的同时使用工具调用，请对 `generateText` 或 `streamText` 使用 `output` 选项。

**重要**：`output` 与工具调用一起使用时，结构化输出生成在执行流程中算作额外一步。

## 解决方案

同时使用 `output` 与工具调用时，请相应调整 `stopWhen` 条件，为结构化输出所需的额外步骤留出空间：

```tsx
const result = await generateText({
  model: __MODEL__,
  output: Output.object({
    schema: z.object({
      summary: z.string(),
      sentiment: z.enum(['positive', 'neutral', 'negative']),
    }),
  }),
  tools: {
    analyze: tool({
      description: 'Analyze data',
      inputSchema: z.object({
        data: z.string(),
      }),
      execute: async ({ data }) => {
        return { result: 'analyzed' };
      }),
    },
  },
  // Add at least 1 to your intended step count to account for structured output
  stopWhen: isStepCount(3), // Now accounts for: tool call + tool result + structured output
  prompt: 'Analyze the data and provide a summary',
});
```

更多关于在 `generateText` 与 `streamText` 中使用结构化输出的说明，见 [生成结构化数据](/docs/ai-sdk-core/generating-structured-data#structured-outputs-with-generatetext-and-streamtext)。
