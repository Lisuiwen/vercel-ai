---
title: 可流式 UI 错误
description: 排查与可流式 UI 相关的错误。
---

# 可流式 UI 组件错误

## 问题

- 变量未找到
- 找不到 `div`
- `Component` 指向的是值，却被当作类型使用

## 解决方案

若在 Server Actions 中使用可流式 UI 时出现上述错误，多半是因为文件扩展名是 `.ts` 而不是 `.tsx`。
