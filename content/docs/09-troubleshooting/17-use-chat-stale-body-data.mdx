---
title: useChat 中 body 值陈旧
description: 排查通过 useChat 的 body 传参时出现值不更新的问题
---

# useChat 中 body 值陈旧

## 问题

使用 `useChat` 并在钩子层通过 `body` 传入会变化的数据时，请求里始终是初次渲染时的值。原因是 body 在钩子初始化时只捕获一次，后续组件重渲染不会更新。

```tsx
// Problematic code - body data will be stale
export default function Chat() {
  const [temperature, setTemperature] = useState(0.7);
  const [userId, setUserId] = useState('user123');

  // This body configuration is captured once and won't update
  const { messages, sendMessage } = useChat({
    transport: new DefaultChatTransport({
      api: '/api/chat',
      body: {
        temperature, // Always the initial value (0.7)
        userId, // Always the initial value ('user123')
      },
    }),
  });

  // Even if temperature or userId change, the body in requests will still use initial values
  return (
    <div>
      <input
        type="range"
        value={temperature}
        onChange={e => setTemperature(parseFloat(e.target.value))}
      />
      {/* Chat UI */}
    </div>
  );
}
```

## 背景

钩子级的 body 只在首次渲染时计算，不会在组件状态变化时重新求值。

## 解决方案

不要把随状态变化的量放在钩子层，改在 `sendMessage` 的第二个参数里传入。请求级选项在每次调用时求值，并优先于钩子级选项。

```tsx
export default function Chat() {
  const [temperature, setTemperature] = useState(0.7);
  const [userId, setUserId] = useState('user123');
  const [input, setInput] = useState('');

  const { messages, sendMessage } = useChat({
    // Static configuration only
    transport: new DefaultChatTransport({
      api: '/api/chat',
    }),
  });

  return (
    <div>
      <input
        type="range"
        value={temperature}
        onChange={e => setTemperature(parseFloat(e.target.value))}
      />

      <form
        onSubmit={event => {
          event.preventDefault();
          if (input.trim()) {
            // Pass dynamic values as request-level options
            sendMessage(
              { text: input },
              {
                body: {
                  temperature, // Current value at request time
                  userId, // Current value at request time
                },
              },
            );
            setInput('');
          }
        }}
      >
        <input value={input} onChange={e => setInput(e.target.value)} />
      </form>
    </div>
  );
}
```

### 备选：钩子层的动态配置

若必须在钩子层响应变化，可使用返回配置的函数。但对组件状态通常需要配合 `useRef` 读取当前值：

```tsx
export default function Chat() {
  const temperatureRef = useRef(0.7);

  const { messages, sendMessage } = useChat({
    transport: new DefaultChatTransport({
      api: '/api/chat',
      body: () => ({
        temperature: temperatureRef.current, // Access via ref.current
        sessionId: getCurrentSessionId(), // Function calls work directly
      }),
    }),
  });

  // ...
}
```

**建议：**对会随组件生命周期变化的动态值，请求级配置更简单可靠。

### 服务端处理

在服务端从请求体解构出自定义字段：

```tsx
// app/api/chat/route.ts
export async function POST(req: Request) {
  const { messages, temperature, userId } = await req.json();

  const result = streamText({
    model: 'openai/gpt-5-mini',
    messages: await convertToModelMessages(messages),
    temperature, // Use the dynamic temperature from the request
    // ... other configuration
  });

  return result.toUIMessageStreamResponse();
}
```

更多说明见 [聊天机器人请求配置](/docs/ai-sdk-ui/chatbot#request-configuration)。
