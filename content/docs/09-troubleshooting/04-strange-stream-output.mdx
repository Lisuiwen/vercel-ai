---
title: useChat/useCompletion 流式输出出现 0:... 而非文本
description: 如何修复界面中异常的流式输出
---

# useChat/useCompletion 流式输出出现 `0:...` 而非文本

## 问题

我使用自定义客户端代码处理通过 `StreamingTextResponse` 发送的服务端响应。AI SDK 版本为 `3.0.20` 或更新。发送查询后，界面流式显示的是 `0: "Je"`、`0: " suis"`、`0: "des"...` 这类内容，而不是期望的纯文本。

## 背景

AI SDK 自 `3.0.20` 起切换到流数据协议，会发送不同的流片段以支持数据、工具调用等。你看到的是原始流数据协议的响应。

## 解决方案

你可以任选其一：

1. 使用 AI Core 的 [`streamText`](/docs/reference/ai-sdk-core/stream-text) 发送原始文本流：

   ```tsx
   export async function POST(req: Request) {
     const { prompt } = await req.json();

     const result = streamText({
       model: openai.completion('gpt-3.5-turbo-instruct'),
       maxOutputTokens: 2000,
       prompt,
     });

     return result.toTextStreamResponse();
   }
   ```

2. 将 AI SDK 版本固定在 `3.0.19`，这样会保持原始文本流。
