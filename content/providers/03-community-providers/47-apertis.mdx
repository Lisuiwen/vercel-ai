---
title: Apertis
description: 适用于 AI SDK 的 Apertis 提供商
---

# Apertis 提供商

[Apertis](https://apertis.ai) 是统一 AI 网关，通过单一 API 访问来自 OpenAI、Anthropic、Google 等厂商的 470+ 模型。

## 核心特性

- **一把 API 密钥**：用单一密钥访问多家提供商的 470+ 模型
- **按量付费**：定价透明，无月费
- **企业就绪**：高可用基础设施与自动故障转移
- **OpenAI 兼容**：可作为 OpenAI API 的直接替代
- **紧跟新模型**：新模型发布即可使用

## 安装与配置

<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tab>
    <Snippet text="pnpm add @apertis/ai-sdk-provider" dark />
  </Tab>
  <Tab>
    <Snippet text="npm install @apertis/ai-sdk-provider" dark />
  </Tab>
  <Tab>
    <Snippet text="yarn add @apertis/ai-sdk-provider" dark />
  </Tab>
</Tabs>

## 提供商实例

可导入默认 `apertis` 实例，或创建自定义实例：

```ts
import { apertis } from '@apertis/ai-sdk-provider';
```

或使用 API 密钥创建自定义实例：

```ts
import { createApertis } from '@apertis/ai-sdk-provider';

const apertis = createApertis({
  apiKey: process.env.APERTIS_API_KEY,
});
```

在 [Apertis 控制台](https://apertis.ai/token) 获取 API 密钥。

## 语言模型

使用 `apertis()` 或 `apertis.chat()` 访问聊天模型：

```ts
const model = apertis('gpt-5.2');
// or
const model = apertis.chat('claude-sonnet-4.5');
```

### 支持的模型

- **OpenAI**：`gpt-5.2`、`gpt-5.2-chat`、`gpt-5.2-pro`
- **Anthropic**：`claude-opus-4-5-20251101`、`claude-sonnet-4.5`、`claude-haiku-4.5`
- **Google**：`gemini-3.1-pro-preview`、`gemini-3-flash-preview`、`gemini-2.5-pro`
- **其他**：`glm-4.7`、`minimax-m2.1` 等 470+ 模型

## 嵌入模型

使用 `apertis.textEmbeddingModel()` 创建文本嵌入：

```ts
const embeddingModel = apertis.textEmbeddingModel('text-embedding-3-small');
```

### 支持的嵌入模型

- `text-embedding-3-small`
- `text-embedding-3-large`
- `text-embedding-ada-002`

## 示例

### 生成文本

```ts
import { apertis } from '@apertis/ai-sdk-provider';
import { generateText } from 'ai';

const { text } = await generateText({
  model: apertis('gpt-5.2'),
  prompt: 'Explain quantum computing in simple terms.',
});
```

### 流式文本

```ts
import { apertis } from '@apertis/ai-sdk-provider';
import { streamText } from 'ai';

const { textStream } = await streamText({
  model: apertis('claude-sonnet-4.5'),
  prompt: 'Write a haiku about programming.',
});

for await (const chunk of textStream) {
  process.stdout.write(chunk);
}
```

### 生成嵌入

```ts
import { apertis } from '@apertis/ai-sdk-provider';
import { embed } from 'ai';

const { embedding } = await embed({
  model: apertis.textEmbeddingModel('text-embedding-3-small'),
  value: 'Hello world',
});
```

## 更多资源

- [GitHub 仓库](https://github.com/apertis-ai/apertis-sdk)
- [Apertis 文档](https://docs.apertis.ai)
- [Apertis 控制台](https://apertis.ai/token)
