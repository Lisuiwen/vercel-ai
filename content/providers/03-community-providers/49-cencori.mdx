---
title: Cencori
description: 适用于 AI SDK 的 Cencori 提供商
---

# Cencori

[Cencori](https://cencori.com) 是 AI 应用的统一基础设施层，通过单一 API 提供内置安全、可观测性与多提供商支持。

使用 Cencori 可获得：

- **多提供商网关**：通过同一 API 访问 OpenAI、Anthropic、Google Gemini、Mistral、DeepSeek 等
- **内置安全**：自动 PII 检测、提示注入防护与内容过滤
- **完整可观测性**：每次请求的审计日志、分析与成本追踪
- **透明计费**：基于额度计费，成本实时可见

## 安装与配置

Cencori 提供商位于 `cencori` 模块。可使用以下命令安装：

<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tab>```bash pnpm add cencori ```</Tab>
  <Tab>```bash npm install cencori ```</Tab>
  <Tab>```bash yarn add cencori ```</Tab>
</Tabs>

## 提供商实例

使用 `createCencori` 创建 Cencori 提供商实例：

```ts
import { createCencori } from 'cencori/vercel';

const cencori = createCencori({
  apiKey: 'YOUR_CENCORI_API_KEY',
});
```

可在 [Cencori 控制台](https://cencori.com/dashboard) 获取 API 密钥。

### 环境变量

也可设置 `CENCORI_API_KEY` 环境变量并使用默认提供商：

```ts
import { cencori } from 'cencori/vercel';

// Uses CENCORI_API_KEY environment variable
const model = cencori('gpt-4o');
```

## 语言模型

Cencori 提供多家提供商的语言模型。直接使用提供商函数创建模型：

```ts
// OpenAI models
const gpt4o = cencori('gpt-4o');
const gpt4oMini = cencori('gpt-4o-mini');

// Anthropic models
const claude = cencori('claude-3-5-sonnet');
const opus = cencori('claude-3-opus');

// Google Gemini models
const gemini = cencori('gemini-2.5-flash');
const geminiPro = cencori('gemini-3.1-pro-preview');

// Other providers
const mistral = cencori('mistral-large');
const deepseek = cencori('deepseek-v3.2');
const grok = cencori('grok-4');
const llama = cencori('llama-3-70b');
```

完整模型列表见 [Cencori 文档](https://cencori.com/docs)。

## 示例

以下示例展示如何将 Cencori 与 AI SDK 配合使用：

### generateText

```ts
import { cencori } from 'cencori/vercel';
import { generateText } from 'ai';

const { text } = await generateText({
  model: cencori('gpt-4o'),
  prompt: 'What is Cencori?',
});

console.log(text);
```

### streamText

```ts
import { cencori } from 'cencori/vercel';
import { streamText } from 'ai';

const result = streamText({
  model: cencori('claude-3-5-sonnet'),
  prompt: 'Write a short story about AI safety.',
});

for await (const chunk of result.textStream) {
  console.log(chunk);
}
```

### 工具调用

Cencori 在兼容模型上支持工具调用：

```ts
import { cencori } from 'cencori/vercel';
import { generateText, tool } from 'ai';
import { z } from 'zod';

const { text, toolCalls } = await generateText({
  model: cencori('gpt-4o'),
  prompt: 'What is the weather in San Francisco?',
  tools: {
    getWeather: tool({
      description: 'Get the weather for a location',
      parameters: z.object({
        location: z.string().describe('The city and state'),
      }),
      execute: async ({ location }) => {
        return { temperature: 72, condition: 'sunny', location };
      },
    }),
  },
});

console.log(text, toolCalls);
```

### Next.js 路由处理器

```ts
import { cencori } from 'cencori/vercel';
import { streamText } from 'ai';

export async function POST(req: Request) {
  const { messages } = await req.json();

  const result = streamText({
    model: cencori('gemini-2.5-flash'),
    messages,
  });

  return result.toDataStreamResponse();
}
```

### useChat Hook

```tsx
'use client';

import { useChat } from '@ai-sdk/react';

export default function Chat() {
  const { messages, input, handleInputChange, handleSubmit } = useChat({
    api: '/api/chat',
  });

  return (
    <div>
      {messages.map(m => (
        <div key={m.id}>
          {m.role}: {m.content}
        </div>
      ))}
      <form onSubmit={handleSubmit}>
        <input value={input} onChange={handleInputChange} />
        <button type="submit">Send</button>
      </form>
    </div>
  );
}
```

## 高级功能

Cencori 提供多项高级能力以增强 AI 应用：

1. **内置安全**：每次请求经过自动安全检查，包括 PII 检测、提示注入防护与有害内容过滤。违反安全策略的请求会在到达模型前被拦截。

2. **多提供商路由**：无需改代码即可在 15+ 提供商（OpenAI、Anthropic、Google、Mistral、DeepSeek、xAI 等）间切换，路由由 Cencori 自动处理。

3. **成本追踪**：每次请求实时追踪成本与分析。可在控制台按模型、提供商与项目查看明细。

4. **完整审计日志**：每次请求记录完整载荷、token 用量、成本与安全分数，便于合规与调试。

5. **自带密钥（BYOK）**：可为各提供商使用自有 API 密钥，同时仍享受 Cencori 的安全与可观测层。

6. **提供商故障转移**：主提供商不可用时自动切换到备用提供商。

更多信息与高级配置见 [Cencori 文档](https://cencori.com/docs)。

## 提供商选项

可使用额外选项自定义提供商：

```ts
import { createCencori } from 'cencori/vercel';

const cencori = createCencori({
  apiKey: 'YOUR_CENCORI_API_KEY',
  baseUrl: 'https://cencori.com', // Custom base URL (optional)
  headers: {
    // Custom headers (optional)
    'X-Custom-Header': 'value',
  },
});
```

### 模型专属选项

也可传入模型专属选项：

```ts
const model = cencori('gpt-4o', {
  userId: 'user-123', // Track usage by user
});
```

## 更多资源

- [Cencori 提供商仓库](https://github.com/cencori/cencori)
- [Cencori 文档](https://cencori.com/docs)
- [Cencori 控制台](https://cencori.com/dashboard/organizations)
- [NPM 包](https://www.npmjs.com/package/cencori)
