---
title: LangSmith
description: 使用 LangSmith 监控与评估 AI SDK 应用
---

# LangSmith 可观测性

[LangSmith](https://docs.langchain.com/langsmith/) 是构建生产级 LLM 应用的平台。
它支持你密切监控并评估应用，从而快速且有信心地上线。

不必使用 LangChain 的开源框架。

<Note>
  [LangSmith 文档](https://docs.langchain.com/langsmith/trace-with-vercel-ai-sdk) 中亦有本指南版本。
  若你使用 AI SDK v4 与较旧版本的 `langsmith` 客户端，请参阅该页面链接的旧版指南。
</Note>

## 设置

<Note>本指南假定你使用 `langsmith>=0.3.63.`。</Note>

安装某一 [AI SDK model provider](/providers/ai-sdk-providers) 与 [LangSmith client SDK](https://npmjs.com/package/langsmith)。
下文示例使用 [AI SDK 的 OpenAI provider](/providers/ai-sdk-providers/openai)，你也可以使用任一 [其它支持的 provider](/providers/ai-sdk-providers)。

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab>
    <Snippet text="pnpm add @ai-sdk/openai langsmith" dark />
  </Tab>
  <Tab>
    <Snippet text="npm install @ai-sdk/openai langsmith" dark />
  </Tab>
  <Tab>
    <Snippet text="yarn add @ai-sdk/openai langsmith" dark />
  </Tab>
  <Tab>
    <Snippet text="bun add @ai-sdk/openai langsmith" dark />
  </Tab>
</Tabs>

接下来设置所需环境变量。

```bash
export LANGCHAIN_TRACING=true
export LANGCHAIN_API_KEY=<your-api-key>

export OPENAI_API_KEY=<your-openai-api-key> # The examples use OpenAI (replace with your selected provider)
```

## Trace 日志

要开始追踪，需在代码入口处导入并调用 `wrapAISDK`：

```ts highlight="6-7"
import { openai } from '@ai-sdk/openai';
import * as ai from 'ai';

import { wrapAISDK } from 'langsmith/experimental/vercel';

const { generateText, streamText } = wrapAISDK(ai);

await generateText({
  model: openai('gpt-5-nano'),
  prompt: 'Write a vegetarian lasagna recipe for 4 people.',
});
```

你应在 LangSmith 控制台中看到类似 [此示例](https://smith.langchain.com/public/4f0e689e-c801-44d3-8857-93b47ab100cc/r) 的 trace。

也可追踪包含工具调用的运行：

```ts
import * as ai from 'ai';
import { tool, isStepCount } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

import { wrapAISDK } from 'langsmith/experimental/vercel';

const { generateText, streamText } = wrapAISDK(ai);

await generateText({
  model: openai('gpt-5-nano'),
  messages: [
    {
      role: 'user',
      content: 'What are my orders and where are they? My user ID is 123',
    },
  ],
  tools: {
    listOrders: tool({
      description: 'list all orders',
      inputSchema: z.object({ userId: z.string() }),
      execute: async ({ userId }) =>
        `User ${userId} has the following orders: 1`,
    }),
    viewTrackingInformation: tool({
      description: 'view tracking information for a specific order',
      inputSchema: z.object({ orderId: z.string() }),
      execute: async ({ orderId }) =>
        `Here is the tracking information for ${orderId}`,
    }),
  },
  stopWhen: isStepCount(5),
});
```

会得到类似 [此 trace](https://smith.langchain.com/public/6075fa2c-d255-4885-a66a-4fc798afaa9f/r) 的结果。

你可照常使用其它 AI SDK 方法。

### 配合 `traceable`

可在 AI SDK 调用外围或工具调用内部包裹 `traceable`。若要在 LangSmith 中将多次运行分组，这会很有用：

```ts
import * as ai from 'ai';
import { tool, isStepCount } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

import { traceable } from 'langsmith/traceable';
import { wrapAISDK } from 'langsmith/experimental/vercel';

const { generateText, streamText } = wrapAISDK(ai);

const wrapper = traceable(
  async (input: string) => {
    const { text } = await generateText({
      model: openai('gpt-5-nano'),
      messages: [
        {
          role: 'user',
          content: input,
        },
      ],
      tools: {
        listOrders: tool({
          description: 'list all orders',
          inputSchema: z.object({ userId: z.string() }),
          execute: async ({ userId }) =>
            `User ${userId} has the following orders: 1`,
        }),
        viewTrackingInformation: tool({
          description: 'view tracking information for a specific order',
          inputSchema: z.object({ orderId: z.string() }),
          execute: async ({ orderId }) =>
            `Here is the tracking information for ${orderId}`,
        }),
      },
      stopWhen: isStepCount(5),
    });
    return text;
  },
  {
    name: 'wrapper',
  },
);

await wrapper('What are my orders and where are they? My user ID is 123.');
```

生成的 trace 类似 [此示例](https://smith.langchain.com/public/ff25bc26-9389-4798-8b91-2bdcc95d4a8e/r)。

## 在无服务器环境中的追踪

在无服务器环境中追踪时，必须在环境关停前等待所有 run 完成 flush。示例见 LangSmith 文档 [此小节](https://docs.langchain.com/langsmith/trace-with-vercel-ai-sdk#tracing-in-serverless-environments)。

## 延伸阅读

更多示例与在具体环境中配置 tracing 的说明：

- [LangSmith docs](https://docs.langchain.com/langsmith/)
- [LangSmith guide on tracing with the AI SDK](https://docs.langchain.com/langsmith/trace-with-vercel-ai-sdk)

为项目配置 LangSmith tracing 后，可尝试采集数据集并进行评估：

- [LangSmith evaluation](https://docs.langchain.com/langsmith/evaluation)
