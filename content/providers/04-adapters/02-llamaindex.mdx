---
title: LlamaIndex
description: 了解如何将 LlamaIndex 与 AI SDK 一起使用。
---

# LlamaIndex

[LlamaIndex](https://ts.llamaindex.ai/) 是用于构建 LLM 驱动应用的框架。LlamaIndex 帮助你摄取、结构化并访问私有或领域特定数据。LlamaIndex.TS 为 Node.js（官方支持）、Vercel Edge Functions（实验性）和 Deno（实验性）等常用运行时提供 Python 版 LlamaIndex 的核心能力。

## 示例：补全

下面是一个在 [Next.js](https://nextjs.org/docs) App Router 中同时使用 AI SDK 与 LlamaIndex 的基础示例。

AI SDK 的 `@ai-sdk/llamaindex` 包会调用 [LlamaIndex ChatEngine](https://ts.llamaindex.ai/modules/chat_engine) 上的 `chat` 方法或 [LlamaIndex QueryEngine](https://ts.llamaindex.ai/modules/query_engines) 上的 `query` 方法所得到的流式结果，将文本管道传输到客户端。

```tsx filename="app/api/completion/route.ts" highlight="17"
import { OpenAI, SimpleChatEngine } from 'llamaindex';
import { toUIMessageStream } from '@ai-sdk/llamaindex';
import { createUIMessageStreamResponse } from 'ai';

export const maxDuration = 60;

export async function POST(req: Request) {
  const { prompt } = await req.json();

  const llm = new OpenAI({ model: 'gpt-4o' });
  const chatEngine = new SimpleChatEngine({ llm });

  const stream = await chatEngine.chat({
    message: prompt,
    stream: true,
  });

  return createUIMessageStreamResponse({
    stream: toUIMessageStream(stream),
  });
}
```

然后，在页面组件中使用 AI SDK 的 [`useCompletion`](/docs/ai-sdk-ui/completion) 方法处理补全：

```tsx filename="app/page.tsx"
'use client';

import { useCompletion } from '@ai-sdk/react';

export default function Chat() {
  const { completion, input, handleInputChange, handleSubmit } =
    useCompletion();

  return (
    <div>
      {completion}
      <form onSubmit={handleSubmit}>
        <input value={input} onChange={handleInputChange} />
      </form>
    </div>
  );
}
```

## 更多示例

[create-llama](https://github.com/run-llama/create-llama) 是上手 LlamaIndex 的最简单方式。其生成的所有代码均通过 AI SDK 连接 LlamaIndex。
